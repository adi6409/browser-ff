//
// Automatically generated by ipdlc.
// Edit at your own risk
//


#include "mozilla/dom/PURLClassifierParent.h"
#include "mozilla/ProfilerLabels.h"
#include "mozilla/dom/URLClassifierParent.h"

#include "mozilla/dom/PContentParent.h"

#include "ipc/IPCMessageUtils.h"
#include "nsIFile.h"
#include "mozilla/ipc/Endpoint.h"
#include "mozilla/ipc/ProtocolMessageUtils.h"
#include "mozilla/ipc/ProtocolUtils.h"
#include "mozilla/ipc/ShmemMessageUtils.h"
#include "mozilla/ipc/TaintingIPCUtils.h"

namespace mozilla {
namespace dom {


MOZ_IMPLICIT PURLClassifierParent::PURLClassifierParent() :
    mozilla::ipc::IProtocol(PURLClassifierMsgStart, mozilla::ipc::ParentSide)
{
    MOZ_COUNT_CTOR(PURLClassifierParent);
}

PURLClassifierParent::~PURLClassifierParent()
{
    MOZ_COUNT_DTOR(PURLClassifierParent);
}

auto PURLClassifierParent::Manager() const -> PContentParent*
{
    return static_cast<PContentParent*>(IProtocol::Manager());
}

auto PURLClassifierParent::AllManagedActors(nsTArray<RefPtr<mozilla::ipc::ActorLifecycleProxy>>& arr__) const -> void
{
    uint32_t total = 0;
    arr__.SetCapacity(total);

}

auto PURLClassifierParent::Send__delete__(
        PURLClassifierParent* actor,
        const mozilla::Maybe<ClassifierInfo>& info,
        const nsresult& errorCode) -> bool
{
    if (!actor || !actor->CanSend()) {
        NS_WARNING("Attempt to __delete__ missing or closed actor");
        return false;
    }
    IPC::Message* msg__ = PURLClassifier::Msg___delete__((actor)->Id());

    MOZ_RELEASE_ASSERT(actor, "NULL actor value passed to non-nullable param");
    WriteIPDLParam(msg__, actor, actor);
    // Sentinel = 'actor'
    (msg__)->WriteSentinel(102892058);
    WriteIPDLParam(msg__, actor, info);
    // Sentinel = 'info'
    (msg__)->WriteSentinel(70058413);
    WriteIPDLParam(msg__, actor, errorCode);
    // Sentinel = 'errorCode'
    (msg__)->WriteSentinel(312345510);



    if (mozilla::ipc::LoggingEnabledFor("PURLClassifierParent")) {
        mozilla::ipc::LogMessageForProtocol(
            "PURLClassifierParent",
            (actor)->OtherPid(),
            "Sending ",
            msg__->type(),
            mozilla::ipc::MessageDirection::eSending);
    }
    AUTO_PROFILER_LABEL("PURLClassifier::Msg___delete__", OTHER);

    bool sendok__ = (actor)->ChannelSend(msg__);

    IProtocol* mgr = actor->Manager();
    actor->DestroySubtree(Deletion);
    actor->ClearSubtree();
    mgr->RemoveManagee(PURLClassifierMsgStart, actor);
    return sendok__;
}

auto PURLClassifierParent::RemoveManagee(
        int32_t aProtocolId,
        IProtocol* aListener) -> void
{
    FatalError("unreached");
    return;
}

auto PURLClassifierParent::DeallocManagee(
        int32_t aProtocolId,
        IProtocol* aListener) -> void
{
    FatalError("unreached");
    return;
}

auto PURLClassifierParent::OnMessageReceived(const Message& msg__) -> PURLClassifierParent::Result
{
    switch (msg__.type()) {
    case PURLClassifier::Reply___delete____ID:
        {
            return MsgProcessed;
        }
    default:
        return MsgNotKnown;
    }
}

auto PURLClassifierParent::OnMessageReceived(
        const Message& msg__,
        Message*& reply__) -> PURLClassifierParent::Result
{
    return MsgNotKnown;
}

auto PURLClassifierParent::OnCallReceived(
        const Message& msg__,
        Message*& reply__) -> PURLClassifierParent::Result
{
    MOZ_ASSERT_UNREACHABLE("message protocol not supported");
    return MsgNotKnown;
}

auto PURLClassifierParent::ClearSubtree() -> void
{
}



} // namespace dom
} // namespace mozilla
namespace mozilla {
namespace ipc {
auto IPDLParamTraits<mozilla::dom::PURLClassifierParent*>::Write(
        IPC::Message* aMsg,
        mozilla::ipc::IProtocol* aActor,
        const paramType& aVar) -> void
{
    int32_t id;
    if (!aVar) {
        id = 0;  // kNullActorId
    } else {
        id = aVar->Id();
        if (id == 1) {  // kFreedActorId
            aVar->FatalError("Actor has been |delete|d");
        }
        MOZ_RELEASE_ASSERT(
            aActor->GetIPCChannel() == aVar->GetIPCChannel(),
            "Actor must be from the same channel as the"
            " actor it's being sent over");
        MOZ_RELEASE_ASSERT(
            aVar->CanSend(),
            "Actor must still be open when sending");
    }

    WriteIPDLParam(aMsg, aActor, id);
}

auto IPDLParamTraits<mozilla::dom::PURLClassifierParent*>::Read(
        const IPC::Message* aMsg,
        PickleIterator* aIter,
        mozilla::ipc::IProtocol* aActor,
        paramType* aVar) -> bool
{
    mozilla::Maybe<mozilla::ipc::IProtocol*> actor =
        aActor->ReadActor(aMsg, aIter, true, "PURLClassifier", PURLClassifierMsgStart);
    if (actor.isNothing()) {
        return false;
    }

    *aVar = static_cast<mozilla::dom::PURLClassifierParent*>(actor.value());
    return true;
}

} // namespace ipc
} // namespace mozilla
