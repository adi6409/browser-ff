//
// Automatically generated by ipdlc.
// Edit at your own risk
//


#include "mozilla/dom/PTemporaryIPCBlobChild.h"
#include "mozilla/ProfilerLabels.h"
#include "mozilla/dom/TemporaryIPCBlobChild.h"

#include "mozilla/ipc/PBackgroundChild.h"
#include "mozilla/ipc/PChildToParentStreamChild.h"
#include "mozilla/ipc/PFileDescriptorSetChild.h"
#include "mozilla/ipc/PParentToChildStreamChild.h"
#include "mozilla/PRemoteLazyInputStreamChild.h"

#include "ipc/IPCMessageUtils.h"
#include "nsIFile.h"
#include "mozilla/ipc/Endpoint.h"
#include "mozilla/ipc/ProtocolMessageUtils.h"
#include "mozilla/ipc/ProtocolUtils.h"
#include "mozilla/ipc/ShmemMessageUtils.h"
#include "mozilla/ipc/TaintingIPCUtils.h"

namespace mozilla {
namespace dom {


MOZ_IMPLICIT PTemporaryIPCBlobChild::PTemporaryIPCBlobChild() :
    mozilla::ipc::IProtocol(PTemporaryIPCBlobMsgStart, mozilla::ipc::ChildSide)
{
    MOZ_COUNT_CTOR(PTemporaryIPCBlobChild);
}

PTemporaryIPCBlobChild::~PTemporaryIPCBlobChild()
{
    MOZ_COUNT_DTOR(PTemporaryIPCBlobChild);
}

auto PTemporaryIPCBlobChild::Manager() const -> PBackgroundChild*
{
    return static_cast<PBackgroundChild*>(IProtocol::Manager());
}

auto PTemporaryIPCBlobChild::AllManagedActors(nsTArray<RefPtr<mozilla::ipc::ActorLifecycleProxy>>& arr__) const -> void
{
    uint32_t total = 0;
    arr__.SetCapacity(total);

}

auto PTemporaryIPCBlobChild::SendOperationFailed() -> bool
{
    IPC::Message* msg__ = PTemporaryIPCBlob::Msg_OperationFailed(Id());





    if (mozilla::ipc::LoggingEnabledFor("PTemporaryIPCBlobChild")) {
        mozilla::ipc::LogMessageForProtocol(
            "PTemporaryIPCBlobChild",
            OtherPid(),
            "Sending ",
            msg__->type(),
            mozilla::ipc::MessageDirection::eSending);
    }
    AUTO_PROFILER_LABEL("PTemporaryIPCBlob::Msg_OperationFailed", OTHER);

    bool sendok__ = ChannelSend(msg__);
    return sendok__;
}

auto PTemporaryIPCBlobChild::SendOperationDone(
        const nsCString& aContentType,
        const FileDescriptor& aFD) -> bool
{
    IPC::Message* msg__ = PTemporaryIPCBlob::Msg_OperationDone(Id());

    WriteIPDLParam(msg__, this, aContentType);
    // Sentinel = 'aContentType'
    (msg__)->WriteSentinel(518522079);
    WriteIPDLParam(msg__, this, aFD);
    // Sentinel = 'aFD'
    (msg__)->WriteSentinel(32899308);




    if (mozilla::ipc::LoggingEnabledFor("PTemporaryIPCBlobChild")) {
        mozilla::ipc::LogMessageForProtocol(
            "PTemporaryIPCBlobChild",
            OtherPid(),
            "Sending ",
            msg__->type(),
            mozilla::ipc::MessageDirection::eSending);
    }
    AUTO_PROFILER_LABEL("PTemporaryIPCBlob::Msg_OperationDone", OTHER);

    bool sendok__ = ChannelSend(msg__);
    return sendok__;
}

auto PTemporaryIPCBlobChild::RemoveManagee(
        int32_t aProtocolId,
        IProtocol* aListener) -> void
{
    FatalError("unreached");
    return;
}

auto PTemporaryIPCBlobChild::DeallocManagee(
        int32_t aProtocolId,
        IProtocol* aListener) -> void
{
    FatalError("unreached");
    return;
}

auto PTemporaryIPCBlobChild::OnMessageReceived(const Message& msg__) -> PTemporaryIPCBlobChild::Result
{
    switch (msg__.type()) {
    case PTemporaryIPCBlob::Msg_FileDesc__ID:
        {
            if (mozilla::ipc::LoggingEnabledFor("PTemporaryIPCBlobChild")) {
                mozilla::ipc::LogMessageForProtocol(
                    "PTemporaryIPCBlobChild",
                    OtherPid(),
                    "Received ",
                    (&(msg__))->type(),
                    mozilla::ipc::MessageDirection::eReceiving);
            }
            AUTO_PROFILER_LABEL("PTemporaryIPCBlob::Msg_FileDesc", OTHER);

            PickleIterator iter__{msg__};
            FileDescriptor aFD{};

            if ((!(ReadIPDLParam((&(msg__)), (&(iter__)), this, (&(aFD)))))) {
                FatalError("Error deserializing 'FileDescriptor'");
                return MsgValueError;
            }
            // Sentinel = 'aFD'
            if ((!(((&(msg__)))->ReadSentinel((&(iter__)), 32899308)))) {
                mozilla::ipc::SentinelReadError("Error deserializing 'FileDescriptor'");
                return MsgValueError;
            }
            msg__.EndRead(iter__, msg__.type());
            if ((!((static_cast<TemporaryIPCBlobChild*>(this))->RecvFileDesc(std::move(aFD))))) {
                mozilla::ipc::ProtocolErrorBreakpoint("Handler returned error code!");
                // Error handled in mozilla::ipc::IPCResult
                return MsgProcessingError;
            }

            return MsgProcessed;
        }
    case PTemporaryIPCBlob::Msg___delete____ID:
        {
            if (mozilla::ipc::LoggingEnabledFor("PTemporaryIPCBlobChild")) {
                mozilla::ipc::LogMessageForProtocol(
                    "PTemporaryIPCBlobChild",
                    OtherPid(),
                    "Received ",
                    (&(msg__))->type(),
                    mozilla::ipc::MessageDirection::eReceiving);
            }
            AUTO_PROFILER_LABEL("PTemporaryIPCBlob::Msg___delete__", OTHER);

            PickleIterator iter__{msg__};
            PTemporaryIPCBlobChild* actor{};
            IPCBlobOrError aBlobOrError{};

            if ((!(ReadIPDLParam((&(msg__)), (&(iter__)), this, (&(actor)))))) {
                FatalError("Error deserializing 'PTemporaryIPCBlob'");
                return MsgValueError;
            }
            if ((!((*((&(actor))))))) {
                FatalError("Error deserializing 'PTemporaryIPCBlob'");
                return MsgValueError;
            }
            // Sentinel = 'actor'
            if ((!(((&(msg__)))->ReadSentinel((&(iter__)), 102892058)))) {
                mozilla::ipc::SentinelReadError("Error deserializing 'PTemporaryIPCBlob'");
                return MsgValueError;
            }
            if ((!(ReadIPDLParam((&(msg__)), (&(iter__)), this, (&(aBlobOrError)))))) {
                FatalError("Error deserializing 'IPCBlobOrError'");
                return MsgValueError;
            }
            // Sentinel = 'aBlobOrError'
            if ((!(((&(msg__)))->ReadSentinel((&(iter__)), 490276012)))) {
                mozilla::ipc::SentinelReadError("Error deserializing 'IPCBlobOrError'");
                return MsgValueError;
            }
            msg__.EndRead(iter__, msg__.type());
            if ((!((static_cast<TemporaryIPCBlobChild*>(this))->Recv__delete__(std::move(aBlobOrError))))) {
                mozilla::ipc::ProtocolErrorBreakpoint("Handler returned error code!");
                // Error handled in mozilla::ipc::IPCResult
                return MsgProcessingError;
            }


            IProtocol* mgr = actor->Manager();
            actor->DestroySubtree(Deletion);
            actor->ClearSubtree();
            mgr->RemoveManagee(PTemporaryIPCBlobMsgStart, actor);

            return MsgProcessed;
        }
    default:
        return MsgNotKnown;
    }
}

auto PTemporaryIPCBlobChild::OnMessageReceived(
        const Message& msg__,
        Message*& reply__) -> PTemporaryIPCBlobChild::Result
{
    return MsgNotKnown;
}

auto PTemporaryIPCBlobChild::OnCallReceived(
        const Message& msg__,
        Message*& reply__) -> PTemporaryIPCBlobChild::Result
{
    MOZ_ASSERT_UNREACHABLE("message protocol not supported");
    return MsgNotKnown;
}

auto PTemporaryIPCBlobChild::ClearSubtree() -> void
{
}



} // namespace dom
} // namespace mozilla
namespace mozilla {
namespace ipc {
auto IPDLParamTraits<mozilla::dom::PTemporaryIPCBlobChild*>::Write(
        IPC::Message* aMsg,
        mozilla::ipc::IProtocol* aActor,
        const paramType& aVar) -> void
{
    int32_t id;
    if (!aVar) {
        id = 0;  // kNullActorId
    } else {
        id = aVar->Id();
        if (id == 1) {  // kFreedActorId
            aVar->FatalError("Actor has been |delete|d");
        }
        MOZ_RELEASE_ASSERT(
            aActor->GetIPCChannel() == aVar->GetIPCChannel(),
            "Actor must be from the same channel as the"
            " actor it's being sent over");
        MOZ_RELEASE_ASSERT(
            aVar->CanSend(),
            "Actor must still be open when sending");
    }

    WriteIPDLParam(aMsg, aActor, id);
}

auto IPDLParamTraits<mozilla::dom::PTemporaryIPCBlobChild*>::Read(
        const IPC::Message* aMsg,
        PickleIterator* aIter,
        mozilla::ipc::IProtocol* aActor,
        paramType* aVar) -> bool
{
    mozilla::Maybe<mozilla::ipc::IProtocol*> actor =
        aActor->ReadActor(aMsg, aIter, true, "PTemporaryIPCBlob", PTemporaryIPCBlobMsgStart);
    if (actor.isNothing()) {
        return false;
    }

    *aVar = static_cast<mozilla::dom::PTemporaryIPCBlobChild*>(actor.value());
    return true;
}

} // namespace ipc
} // namespace mozilla
