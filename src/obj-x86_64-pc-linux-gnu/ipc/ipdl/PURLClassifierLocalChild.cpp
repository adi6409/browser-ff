//
// Automatically generated by ipdlc.
// Edit at your own risk
//


#include "mozilla/dom/PURLClassifierLocalChild.h"
#include "mozilla/ProfilerLabels.h"
#include "mozilla/dom/URLClassifierChild.h"
#include "mozilla/ipc/URIUtils.h"
#include "nsIURI.h"

#include "mozilla/dom/PContentChild.h"

#include "ipc/IPCMessageUtils.h"
#include "nsIFile.h"
#include "mozilla/ipc/Endpoint.h"
#include "mozilla/ipc/ProtocolMessageUtils.h"
#include "mozilla/ipc/ProtocolUtils.h"
#include "mozilla/ipc/ShmemMessageUtils.h"
#include "mozilla/ipc/TaintingIPCUtils.h"

namespace mozilla {
namespace dom {


MOZ_IMPLICIT PURLClassifierLocalChild::PURLClassifierLocalChild() :
    mozilla::ipc::IProtocol(PURLClassifierLocalMsgStart, mozilla::ipc::ChildSide)
{
    MOZ_COUNT_CTOR(PURLClassifierLocalChild);
}

PURLClassifierLocalChild::~PURLClassifierLocalChild()
{
    MOZ_COUNT_DTOR(PURLClassifierLocalChild);
}

auto PURLClassifierLocalChild::Manager() const -> PContentChild*
{
    return static_cast<PContentChild*>(IProtocol::Manager());
}

auto PURLClassifierLocalChild::AllManagedActors(nsTArray<RefPtr<mozilla::ipc::ActorLifecycleProxy>>& arr__) const -> void
{
    uint32_t total = 0;
    arr__.SetCapacity(total);

}

auto PURLClassifierLocalChild::RemoveManagee(
        int32_t aProtocolId,
        IProtocol* aListener) -> void
{
    FatalError("unreached");
    return;
}

auto PURLClassifierLocalChild::DeallocManagee(
        int32_t aProtocolId,
        IProtocol* aListener) -> void
{
    FatalError("unreached");
    return;
}

auto PURLClassifierLocalChild::OnMessageReceived(const Message& msg__) -> PURLClassifierLocalChild::Result
{
    switch (msg__.type()) {
    case PURLClassifierLocal::Msg___delete____ID:
        {
            if (mozilla::ipc::LoggingEnabledFor("PURLClassifierLocalChild")) {
                mozilla::ipc::LogMessageForProtocol(
                    "PURLClassifierLocalChild",
                    OtherPid(),
                    "Received ",
                    (&(msg__))->type(),
                    mozilla::ipc::MessageDirection::eReceiving);
            }
            AUTO_PROFILER_LABEL("PURLClassifierLocal::Msg___delete__", OTHER);

            PickleIterator iter__{msg__};
            PURLClassifierLocalChild* actor{};
            nsTArray<URLClassifierLocalResult> results{};

            if ((!(ReadIPDLParam((&(msg__)), (&(iter__)), this, (&(actor)))))) {
                FatalError("Error deserializing 'PURLClassifierLocal'");
                return MsgValueError;
            }
            if ((!((*((&(actor))))))) {
                FatalError("Error deserializing 'PURLClassifierLocal'");
                return MsgValueError;
            }
            // Sentinel = 'actor'
            if ((!(((&(msg__)))->ReadSentinel((&(iter__)), 102892058)))) {
                mozilla::ipc::SentinelReadError("Error deserializing 'PURLClassifierLocal'");
                return MsgValueError;
            }
            if ((!(ReadIPDLParam((&(msg__)), (&(iter__)), this, (&(results)))))) {
                FatalError("Error deserializing 'URLClassifierLocalResult[]'");
                return MsgValueError;
            }
            // Sentinel = 'results'
            if ((!(((&(msg__)))->ReadSentinel((&(iter__)), 204800787)))) {
                mozilla::ipc::SentinelReadError("Error deserializing 'URLClassifierLocalResult[]'");
                return MsgValueError;
            }
            msg__.EndRead(iter__, msg__.type());
            if ((!((static_cast<URLClassifierLocalChild*>(this))->Recv__delete__(std::move(results))))) {
                mozilla::ipc::ProtocolErrorBreakpoint("Handler returned error code!");
                // Error handled in mozilla::ipc::IPCResult
                return MsgProcessingError;
            }


            IProtocol* mgr = actor->Manager();
            actor->DestroySubtree(Deletion);
            actor->ClearSubtree();
            mgr->RemoveManagee(PURLClassifierLocalMsgStart, actor);

            return MsgProcessed;
        }
    default:
        return MsgNotKnown;
    }
}

auto PURLClassifierLocalChild::OnMessageReceived(
        const Message& msg__,
        Message*& reply__) -> PURLClassifierLocalChild::Result
{
    return MsgNotKnown;
}

auto PURLClassifierLocalChild::OnCallReceived(
        const Message& msg__,
        Message*& reply__) -> PURLClassifierLocalChild::Result
{
    MOZ_ASSERT_UNREACHABLE("message protocol not supported");
    return MsgNotKnown;
}

auto PURLClassifierLocalChild::ClearSubtree() -> void
{
}



} // namespace dom
} // namespace mozilla
namespace mozilla {
namespace ipc {
auto IPDLParamTraits<mozilla::dom::PURLClassifierLocalChild*>::Write(
        IPC::Message* aMsg,
        mozilla::ipc::IProtocol* aActor,
        const paramType& aVar) -> void
{
    int32_t id;
    if (!aVar) {
        id = 0;  // kNullActorId
    } else {
        id = aVar->Id();
        if (id == 1) {  // kFreedActorId
            aVar->FatalError("Actor has been |delete|d");
        }
        MOZ_RELEASE_ASSERT(
            aActor->GetIPCChannel() == aVar->GetIPCChannel(),
            "Actor must be from the same channel as the"
            " actor it's being sent over");
        MOZ_RELEASE_ASSERT(
            aVar->CanSend(),
            "Actor must still be open when sending");
    }

    WriteIPDLParam(aMsg, aActor, id);
}

auto IPDLParamTraits<mozilla::dom::PURLClassifierLocalChild*>::Read(
        const IPC::Message* aMsg,
        PickleIterator* aIter,
        mozilla::ipc::IProtocol* aActor,
        paramType* aVar) -> bool
{
    mozilla::Maybe<mozilla::ipc::IProtocol*> actor =
        aActor->ReadActor(aMsg, aIter, true, "PURLClassifierLocal", PURLClassifierLocalMsgStart);
    if (actor.isNothing()) {
        return false;
    }

    *aVar = static_cast<mozilla::dom::PURLClassifierLocalChild*>(actor.value());
    return true;
}

} // namespace ipc
} // namespace mozilla
