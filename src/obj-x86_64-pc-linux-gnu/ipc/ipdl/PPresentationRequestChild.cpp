//
// Automatically generated by ipdlc.
// Edit at your own risk
//


#include "mozilla/dom/PPresentationRequestChild.h"
#include "mozilla/ProfilerLabels.h"
#include "mozilla/dom/PresentationChild.h"

#include "mozilla/dom/PPresentationChild.h"

#include "ipc/IPCMessageUtils.h"
#include "nsIFile.h"
#include "mozilla/ipc/Endpoint.h"
#include "mozilla/ipc/ProtocolMessageUtils.h"
#include "mozilla/ipc/ProtocolUtils.h"
#include "mozilla/ipc/ShmemMessageUtils.h"
#include "mozilla/ipc/TaintingIPCUtils.h"

namespace mozilla {
namespace dom {


MOZ_IMPLICIT PPresentationRequestChild::PPresentationRequestChild() :
    mozilla::ipc::IProtocol(PPresentationRequestMsgStart, mozilla::ipc::ChildSide)
{
    MOZ_COUNT_CTOR(PPresentationRequestChild);
}

PPresentationRequestChild::~PPresentationRequestChild()
{
    MOZ_COUNT_DTOR(PPresentationRequestChild);
}

auto PPresentationRequestChild::Manager() const -> PPresentationChild*
{
    return static_cast<PPresentationChild*>(IProtocol::Manager());
}

auto PPresentationRequestChild::AllManagedActors(nsTArray<RefPtr<mozilla::ipc::ActorLifecycleProxy>>& arr__) const -> void
{
    uint32_t total = 0;
    arr__.SetCapacity(total);

}

auto PPresentationRequestChild::RemoveManagee(
        int32_t aProtocolId,
        IProtocol* aListener) -> void
{
    FatalError("unreached");
    return;
}

auto PPresentationRequestChild::DeallocManagee(
        int32_t aProtocolId,
        IProtocol* aListener) -> void
{
    FatalError("unreached");
    return;
}

auto PPresentationRequestChild::OnMessageReceived(const Message& msg__) -> PPresentationRequestChild::Result
{
    switch (msg__.type()) {
    case PPresentationRequest::Msg___delete____ID:
        {
            if (mozilla::ipc::LoggingEnabledFor("PPresentationRequestChild")) {
                mozilla::ipc::LogMessageForProtocol(
                    "PPresentationRequestChild",
                    OtherPid(),
                    "Received ",
                    (&(msg__))->type(),
                    mozilla::ipc::MessageDirection::eReceiving);
            }
            AUTO_PROFILER_LABEL("PPresentationRequest::Msg___delete__", OTHER);

            PickleIterator iter__{msg__};
            PPresentationRequestChild* actor{};
            nsresult result{};

            if ((!(ReadIPDLParam((&(msg__)), (&(iter__)), this, (&(actor)))))) {
                FatalError("Error deserializing 'PPresentationRequest'");
                return MsgValueError;
            }
            if ((!((*((&(actor))))))) {
                FatalError("Error deserializing 'PPresentationRequest'");
                return MsgValueError;
            }
            // Sentinel = 'actor'
            if ((!(((&(msg__)))->ReadSentinel((&(iter__)), 102892058)))) {
                mozilla::ipc::SentinelReadError("Error deserializing 'PPresentationRequest'");
                return MsgValueError;
            }
            if ((!(ReadIPDLParam((&(msg__)), (&(iter__)), this, (&(result)))))) {
                FatalError("Error deserializing 'nsresult'");
                return MsgValueError;
            }
            // Sentinel = 'result'
            if ((!(((&(msg__)))->ReadSentinel((&(iter__)), 153223840)))) {
                mozilla::ipc::SentinelReadError("Error deserializing 'nsresult'");
                return MsgValueError;
            }
            msg__.EndRead(iter__, msg__.type());
            if ((!((static_cast<PresentationRequestChild*>(this))->Recv__delete__(std::move(result))))) {
                mozilla::ipc::ProtocolErrorBreakpoint("Handler returned error code!");
                // Error handled in mozilla::ipc::IPCResult
                return MsgProcessingError;
            }


            IProtocol* mgr = actor->Manager();
            actor->DestroySubtree(Deletion);
            actor->ClearSubtree();
            mgr->RemoveManagee(PPresentationRequestMsgStart, actor);

            return MsgProcessed;
        }
    case PPresentationRequest::Msg_NotifyRequestUrlSelected__ID:
        {
            if (mozilla::ipc::LoggingEnabledFor("PPresentationRequestChild")) {
                mozilla::ipc::LogMessageForProtocol(
                    "PPresentationRequestChild",
                    OtherPid(),
                    "Received ",
                    (&(msg__))->type(),
                    mozilla::ipc::MessageDirection::eReceiving);
            }
            AUTO_PROFILER_LABEL("PPresentationRequest::Msg_NotifyRequestUrlSelected", OTHER);

            PickleIterator iter__{msg__};
            nsString aUrl{};

            if ((!(ReadIPDLParam((&(msg__)), (&(iter__)), this, (&(aUrl)))))) {
                FatalError("Error deserializing 'nsString'");
                return MsgValueError;
            }
            // Sentinel = 'aUrl'
            if ((!(((&(msg__)))->ReadSentinel((&(iter__)), 64422293)))) {
                mozilla::ipc::SentinelReadError("Error deserializing 'nsString'");
                return MsgValueError;
            }
            msg__.EndRead(iter__, msg__.type());
            if ((!((static_cast<PresentationRequestChild*>(this))->RecvNotifyRequestUrlSelected(std::move(aUrl))))) {
                mozilla::ipc::ProtocolErrorBreakpoint("Handler returned error code!");
                // Error handled in mozilla::ipc::IPCResult
                return MsgProcessingError;
            }

            return MsgProcessed;
        }
    default:
        return MsgNotKnown;
    }
}

auto PPresentationRequestChild::OnMessageReceived(
        const Message& msg__,
        Message*& reply__) -> PPresentationRequestChild::Result
{
    return MsgNotKnown;
}

auto PPresentationRequestChild::OnCallReceived(
        const Message& msg__,
        Message*& reply__) -> PPresentationRequestChild::Result
{
    MOZ_ASSERT_UNREACHABLE("message protocol not supported");
    return MsgNotKnown;
}

auto PPresentationRequestChild::ClearSubtree() -> void
{
}



} // namespace dom
} // namespace mozilla
namespace mozilla {
namespace ipc {
auto IPDLParamTraits<mozilla::dom::PPresentationRequestChild*>::Write(
        IPC::Message* aMsg,
        mozilla::ipc::IProtocol* aActor,
        const paramType& aVar) -> void
{
    int32_t id;
    if (!aVar) {
        id = 0;  // kNullActorId
    } else {
        id = aVar->Id();
        if (id == 1) {  // kFreedActorId
            aVar->FatalError("Actor has been |delete|d");
        }
        MOZ_RELEASE_ASSERT(
            aActor->GetIPCChannel() == aVar->GetIPCChannel(),
            "Actor must be from the same channel as the"
            " actor it's being sent over");
        MOZ_RELEASE_ASSERT(
            aVar->CanSend(),
            "Actor must still be open when sending");
    }

    WriteIPDLParam(aMsg, aActor, id);
}

auto IPDLParamTraits<mozilla::dom::PPresentationRequestChild*>::Read(
        const IPC::Message* aMsg,
        PickleIterator* aIter,
        mozilla::ipc::IProtocol* aActor,
        paramType* aVar) -> bool
{
    mozilla::Maybe<mozilla::ipc::IProtocol*> actor =
        aActor->ReadActor(aMsg, aIter, true, "PPresentationRequest", PPresentationRequestMsgStart);
    if (actor.isNothing()) {
        return false;
    }

    *aVar = static_cast<mozilla::dom::PPresentationRequestChild*>(actor.value());
    return true;
}

} // namespace ipc
} // namespace mozilla
