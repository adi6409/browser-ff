//
// Automatically generated by ipdlc.
// Edit at your own risk
//


#include "mozilla/dom/PFileCreatorChild.h"
#include "mozilla/ProfilerLabels.h"
#include "mozilla/dom/FileCreatorChild.h"

#include "mozilla/ipc/PBackgroundChild.h"
#include "mozilla/ipc/PChildToParentStreamChild.h"
#include "mozilla/ipc/PFileDescriptorSetChild.h"
#include "mozilla/PRemoteLazyInputStreamChild.h"
#include "mozilla/ipc/PParentToChildStreamChild.h"

#include "ipc/IPCMessageUtils.h"
#include "nsIFile.h"
#include "mozilla/ipc/Endpoint.h"
#include "mozilla/ipc/ProtocolMessageUtils.h"
#include "mozilla/ipc/ProtocolUtils.h"
#include "mozilla/ipc/ShmemMessageUtils.h"
#include "mozilla/ipc/TaintingIPCUtils.h"

namespace mozilla {
namespace dom {


MOZ_IMPLICIT PFileCreatorChild::PFileCreatorChild() :
    mozilla::ipc::IProtocol(PFileCreatorMsgStart, mozilla::ipc::ChildSide)
{
    MOZ_COUNT_CTOR(PFileCreatorChild);
}

PFileCreatorChild::~PFileCreatorChild()
{
    MOZ_COUNT_DTOR(PFileCreatorChild);
}

auto PFileCreatorChild::Manager() const -> PBackgroundChild*
{
    return static_cast<PBackgroundChild*>(IProtocol::Manager());
}

auto PFileCreatorChild::AllManagedActors(nsTArray<RefPtr<mozilla::ipc::ActorLifecycleProxy>>& arr__) const -> void
{
    uint32_t total = 0;
    arr__.SetCapacity(total);

}

auto PFileCreatorChild::RemoveManagee(
        int32_t aProtocolId,
        IProtocol* aListener) -> void
{
    FatalError("unreached");
    return;
}

auto PFileCreatorChild::DeallocManagee(
        int32_t aProtocolId,
        IProtocol* aListener) -> void
{
    FatalError("unreached");
    return;
}

auto PFileCreatorChild::OnMessageReceived(const Message& msg__) -> PFileCreatorChild::Result
{
    switch (msg__.type()) {
    case PFileCreator::Msg___delete____ID:
        {
            if (mozilla::ipc::LoggingEnabledFor("PFileCreatorChild")) {
                mozilla::ipc::LogMessageForProtocol(
                    "PFileCreatorChild",
                    OtherPid(),
                    "Received ",
                    (&(msg__))->type(),
                    mozilla::ipc::MessageDirection::eReceiving);
            }
            AUTO_PROFILER_LABEL("PFileCreator::Msg___delete__", OTHER);

            PickleIterator iter__{msg__};
            PFileCreatorChild* actor{};
            FileCreationResult aResult{};

            if ((!(ReadIPDLParam((&(msg__)), (&(iter__)), this, (&(actor)))))) {
                FatalError("Error deserializing 'PFileCreator'");
                return MsgValueError;
            }
            if ((!((*((&(actor))))))) {
                FatalError("Error deserializing 'PFileCreator'");
                return MsgValueError;
            }
            // Sentinel = 'actor'
            if ((!(((&(msg__)))->ReadSentinel((&(iter__)), 102892058)))) {
                mozilla::ipc::SentinelReadError("Error deserializing 'PFileCreator'");
                return MsgValueError;
            }
            if ((!(ReadIPDLParam((&(msg__)), (&(iter__)), this, (&(aResult)))))) {
                FatalError("Error deserializing 'FileCreationResult'");
                return MsgValueError;
            }
            // Sentinel = 'aResult'
            if ((!(((&(msg__)))->ReadSentinel((&(iter__)), 185205473)))) {
                mozilla::ipc::SentinelReadError("Error deserializing 'FileCreationResult'");
                return MsgValueError;
            }
            msg__.EndRead(iter__, msg__.type());
            if ((!((static_cast<FileCreatorChild*>(this))->Recv__delete__(std::move(aResult))))) {
                mozilla::ipc::ProtocolErrorBreakpoint("Handler returned error code!");
                // Error handled in mozilla::ipc::IPCResult
                return MsgProcessingError;
            }


            IProtocol* mgr = actor->Manager();
            actor->DestroySubtree(Deletion);
            actor->ClearSubtree();
            mgr->RemoveManagee(PFileCreatorMsgStart, actor);

            return MsgProcessed;
        }
    default:
        return MsgNotKnown;
    }
}

auto PFileCreatorChild::OnMessageReceived(
        const Message& msg__,
        Message*& reply__) -> PFileCreatorChild::Result
{
    return MsgNotKnown;
}

auto PFileCreatorChild::OnCallReceived(
        const Message& msg__,
        Message*& reply__) -> PFileCreatorChild::Result
{
    MOZ_ASSERT_UNREACHABLE("message protocol not supported");
    return MsgNotKnown;
}

auto PFileCreatorChild::ClearSubtree() -> void
{
}



} // namespace dom
} // namespace mozilla
namespace mozilla {
namespace ipc {
auto IPDLParamTraits<mozilla::dom::PFileCreatorChild*>::Write(
        IPC::Message* aMsg,
        mozilla::ipc::IProtocol* aActor,
        const paramType& aVar) -> void
{
    int32_t id;
    if (!aVar) {
        id = 0;  // kNullActorId
    } else {
        id = aVar->Id();
        if (id == 1) {  // kFreedActorId
            aVar->FatalError("Actor has been |delete|d");
        }
        MOZ_RELEASE_ASSERT(
            aActor->GetIPCChannel() == aVar->GetIPCChannel(),
            "Actor must be from the same channel as the"
            " actor it's being sent over");
        MOZ_RELEASE_ASSERT(
            aVar->CanSend(),
            "Actor must still be open when sending");
    }

    WriteIPDLParam(aMsg, aActor, id);
}

auto IPDLParamTraits<mozilla::dom::PFileCreatorChild*>::Read(
        const IPC::Message* aMsg,
        PickleIterator* aIter,
        mozilla::ipc::IProtocol* aActor,
        paramType* aVar) -> bool
{
    mozilla::Maybe<mozilla::ipc::IProtocol*> actor =
        aActor->ReadActor(aMsg, aIter, true, "PFileCreator", PFileCreatorMsgStart);
    if (actor.isNothing()) {
        return false;
    }

    *aVar = static_cast<mozilla::dom::PFileCreatorChild*>(actor.value());
    return true;
}

} // namespace ipc
} // namespace mozilla
