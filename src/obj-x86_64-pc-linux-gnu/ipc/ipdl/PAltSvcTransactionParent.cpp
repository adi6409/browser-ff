//
// Automatically generated by ipdlc.
// Edit at your own risk
//


#include "mozilla/net/PAltSvcTransactionParent.h"
#include "mozilla/ProfilerLabels.h"
#include "mozilla/net/AltSvcTransactionParent.h"

#include "mozilla/net/PSocketProcessParent.h"

#include "ipc/IPCMessageUtils.h"
#include "nsIFile.h"
#include "mozilla/ipc/Endpoint.h"
#include "mozilla/ipc/ProtocolMessageUtils.h"
#include "mozilla/ipc/ProtocolUtils.h"
#include "mozilla/ipc/ShmemMessageUtils.h"
#include "mozilla/ipc/TaintingIPCUtils.h"

namespace mozilla {
namespace net {


MOZ_IMPLICIT PAltSvcTransactionParent::PAltSvcTransactionParent() :
    mozilla::ipc::IProtocol(PAltSvcTransactionMsgStart, mozilla::ipc::ParentSide)
{
    MOZ_COUNT_CTOR(PAltSvcTransactionParent);
}

PAltSvcTransactionParent::~PAltSvcTransactionParent()
{
    MOZ_COUNT_DTOR(PAltSvcTransactionParent);
}

auto PAltSvcTransactionParent::Manager() const -> PSocketProcessParent*
{
    return static_cast<PSocketProcessParent*>(IProtocol::Manager());
}

auto PAltSvcTransactionParent::AllManagedActors(nsTArray<RefPtr<mozilla::ipc::ActorLifecycleProxy>>& arr__) const -> void
{
    uint32_t total = 0;
    arr__.SetCapacity(total);

}

auto PAltSvcTransactionParent::RemoveManagee(
        int32_t aProtocolId,
        IProtocol* aListener) -> void
{
    FatalError("unreached");
    return;
}

auto PAltSvcTransactionParent::DeallocManagee(
        int32_t aProtocolId,
        IProtocol* aListener) -> void
{
    FatalError("unreached");
    return;
}

auto PAltSvcTransactionParent::OnMessageReceived(const Message& msg__) -> PAltSvcTransactionParent::Result
{
    switch (msg__.type()) {
    case PAltSvcTransaction::Msg___delete____ID:
        {
            if (mozilla::ipc::LoggingEnabledFor("PAltSvcTransactionParent")) {
                mozilla::ipc::LogMessageForProtocol(
                    "PAltSvcTransactionParent",
                    OtherPid(),
                    "Received ",
                    (&(msg__))->type(),
                    mozilla::ipc::MessageDirection::eReceiving);
            }
            AUTO_PROFILER_LABEL("PAltSvcTransaction::Msg___delete__", OTHER);

            PickleIterator iter__{msg__};
            PAltSvcTransactionParent* actor{};
            bool aValidateResult{};

            if ((!(ReadIPDLParam((&(msg__)), (&(iter__)), this, (&(actor)))))) {
                FatalError("Error deserializing 'PAltSvcTransaction'");
                return MsgValueError;
            }
            if ((!((*((&(actor))))))) {
                FatalError("Error deserializing 'PAltSvcTransaction'");
                return MsgValueError;
            }
            // Sentinel = 'actor'
            if ((!(((&(msg__)))->ReadSentinel((&(iter__)), 102892058)))) {
                mozilla::ipc::SentinelReadError("Error deserializing 'PAltSvcTransaction'");
                return MsgValueError;
            }
            if ((!(ReadIPDLParam((&(msg__)), (&(iter__)), this, (&(aValidateResult)))))) {
                FatalError("Error deserializing 'bool'");
                return MsgValueError;
            }
            // Sentinel = 'aValidateResult'
            if ((!(((&(msg__)))->ReadSentinel((&(iter__)), 788661771)))) {
                mozilla::ipc::SentinelReadError("Error deserializing 'bool'");
                return MsgValueError;
            }
            msg__.EndRead(iter__, msg__.type());
            if ((!((static_cast<AltSvcTransactionParent*>(this))->Recv__delete__(std::move(aValidateResult))))) {
                mozilla::ipc::ProtocolErrorBreakpoint("Handler returned error code!");
                // Error handled in mozilla::ipc::IPCResult
                return MsgProcessingError;
            }


            IProtocol* mgr = actor->Manager();
            actor->DestroySubtree(Deletion);
            actor->ClearSubtree();
            mgr->RemoveManagee(PAltSvcTransactionMsgStart, actor);

            return MsgProcessed;
        }
    case PAltSvcTransaction::Msg_OnTransactionClose__ID:
        {
            if (mozilla::ipc::LoggingEnabledFor("PAltSvcTransactionParent")) {
                mozilla::ipc::LogMessageForProtocol(
                    "PAltSvcTransactionParent",
                    OtherPid(),
                    "Received ",
                    (&(msg__))->type(),
                    mozilla::ipc::MessageDirection::eReceiving);
            }
            AUTO_PROFILER_LABEL("PAltSvcTransaction::Msg_OnTransactionClose", OTHER);

            PickleIterator iter__{msg__};
            bool aValidateResult{};

            if ((!(ReadIPDLParam((&(msg__)), (&(iter__)), this, (&(aValidateResult)))))) {
                FatalError("Error deserializing 'bool'");
                return MsgValueError;
            }
            // Sentinel = 'aValidateResult'
            if ((!(((&(msg__)))->ReadSentinel((&(iter__)), 788661771)))) {
                mozilla::ipc::SentinelReadError("Error deserializing 'bool'");
                return MsgValueError;
            }
            msg__.EndRead(iter__, msg__.type());
            if ((!((static_cast<AltSvcTransactionParent*>(this))->RecvOnTransactionClose(std::move(aValidateResult))))) {
                mozilla::ipc::ProtocolErrorBreakpoint("Handler returned error code!");
                // Error handled in mozilla::ipc::IPCResult
                return MsgProcessingError;
            }

            return MsgProcessed;
        }
    default:
        return MsgNotKnown;
    }
}

auto PAltSvcTransactionParent::OnMessageReceived(
        const Message& msg__,
        Message*& reply__) -> PAltSvcTransactionParent::Result
{
    return MsgNotKnown;
}

auto PAltSvcTransactionParent::OnCallReceived(
        const Message& msg__,
        Message*& reply__) -> PAltSvcTransactionParent::Result
{
    MOZ_ASSERT_UNREACHABLE("message protocol not supported");
    return MsgNotKnown;
}

auto PAltSvcTransactionParent::ClearSubtree() -> void
{
}



} // namespace net
} // namespace mozilla
namespace mozilla {
namespace ipc {
auto IPDLParamTraits<mozilla::net::PAltSvcTransactionParent*>::Write(
        IPC::Message* aMsg,
        mozilla::ipc::IProtocol* aActor,
        const paramType& aVar) -> void
{
    int32_t id;
    if (!aVar) {
        id = 0;  // kNullActorId
    } else {
        id = aVar->Id();
        if (id == 1) {  // kFreedActorId
            aVar->FatalError("Actor has been |delete|d");
        }
        MOZ_RELEASE_ASSERT(
            aActor->GetIPCChannel() == aVar->GetIPCChannel(),
            "Actor must be from the same channel as the"
            " actor it's being sent over");
        MOZ_RELEASE_ASSERT(
            aVar->CanSend(),
            "Actor must still be open when sending");
    }

    WriteIPDLParam(aMsg, aActor, id);
}

auto IPDLParamTraits<mozilla::net::PAltSvcTransactionParent*>::Read(
        const IPC::Message* aMsg,
        PickleIterator* aIter,
        mozilla::ipc::IProtocol* aActor,
        paramType* aVar) -> bool
{
    mozilla::Maybe<mozilla::ipc::IProtocol*> actor =
        aActor->ReadActor(aMsg, aIter, true, "PAltSvcTransaction", PAltSvcTransactionMsgStart);
    if (actor.isNothing()) {
        return false;
    }

    *aVar = static_cast<mozilla::net::PAltSvcTransactionParent*>(actor.value());
    return true;
}

} // namespace ipc
} // namespace mozilla
