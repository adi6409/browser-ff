//
// Automatically generated by ipdlc.
// Edit at your own risk
//


#include "mozilla/net/PProxyConfigLookupParent.h"
#include "mozilla/ProfilerLabels.h"
#include "mozilla/net/ProxyConfigLookupParent.h"

#include "mozilla/net/PSocketProcessParent.h"

#include "ipc/IPCMessageUtils.h"
#include "nsIFile.h"
#include "mozilla/ipc/Endpoint.h"
#include "mozilla/ipc/ProtocolMessageUtils.h"
#include "mozilla/ipc/ProtocolUtils.h"
#include "mozilla/ipc/ShmemMessageUtils.h"
#include "mozilla/ipc/TaintingIPCUtils.h"

namespace mozilla {
namespace net {


MOZ_IMPLICIT PProxyConfigLookupParent::PProxyConfigLookupParent() :
    mozilla::ipc::IProtocol(PProxyConfigLookupMsgStart, mozilla::ipc::ParentSide)
{
    MOZ_COUNT_CTOR(PProxyConfigLookupParent);
}

PProxyConfigLookupParent::~PProxyConfigLookupParent()
{
    MOZ_COUNT_DTOR(PProxyConfigLookupParent);
}

auto PProxyConfigLookupParent::Manager() const -> PSocketProcessParent*
{
    return static_cast<PSocketProcessParent*>(IProtocol::Manager());
}

auto PProxyConfigLookupParent::AllManagedActors(nsTArray<RefPtr<mozilla::ipc::ActorLifecycleProxy>>& arr__) const -> void
{
    uint32_t total = 0;
    arr__.SetCapacity(total);

}

auto PProxyConfigLookupParent::Send__delete__(
        PProxyConfigLookupParent* actor,
        const nsTArray<ProxyInfoCloneArgs>& aProxyInfo,
        const nsresult& aResult) -> bool
{
    if (!actor || !actor->CanSend()) {
        NS_WARNING("Attempt to __delete__ missing or closed actor");
        return false;
    }
    IPC::Message* msg__ = PProxyConfigLookup::Msg___delete__((actor)->Id());

    MOZ_RELEASE_ASSERT(actor, "NULL actor value passed to non-nullable param");
    WriteIPDLParam(msg__, actor, actor);
    // Sentinel = 'actor'
    (msg__)->WriteSentinel(102892058);
    WriteIPDLParam(msg__, actor, aProxyInfo);
    // Sentinel = 'aProxyInfo'
    (msg__)->WriteSentinel(370344976);
    WriteIPDLParam(msg__, actor, aResult);
    // Sentinel = 'aResult'
    (msg__)->WriteSentinel(185205473);



    if (mozilla::ipc::LoggingEnabledFor("PProxyConfigLookupParent")) {
        mozilla::ipc::LogMessageForProtocol(
            "PProxyConfigLookupParent",
            (actor)->OtherPid(),
            "Sending ",
            msg__->type(),
            mozilla::ipc::MessageDirection::eSending);
    }
    AUTO_PROFILER_LABEL("PProxyConfigLookup::Msg___delete__", OTHER);

    bool sendok__ = (actor)->ChannelSend(msg__);

    IProtocol* mgr = actor->Manager();
    actor->DestroySubtree(Deletion);
    actor->ClearSubtree();
    mgr->RemoveManagee(PProxyConfigLookupMsgStart, actor);
    return sendok__;
}

auto PProxyConfigLookupParent::RemoveManagee(
        int32_t aProtocolId,
        IProtocol* aListener) -> void
{
    FatalError("unreached");
    return;
}

auto PProxyConfigLookupParent::DeallocManagee(
        int32_t aProtocolId,
        IProtocol* aListener) -> void
{
    FatalError("unreached");
    return;
}

auto PProxyConfigLookupParent::OnMessageReceived(const Message& msg__) -> PProxyConfigLookupParent::Result
{
    switch (msg__.type()) {
    case PProxyConfigLookup::Reply___delete____ID:
        {
            return MsgProcessed;
        }
    default:
        return MsgNotKnown;
    }
}

auto PProxyConfigLookupParent::OnMessageReceived(
        const Message& msg__,
        Message*& reply__) -> PProxyConfigLookupParent::Result
{
    return MsgNotKnown;
}

auto PProxyConfigLookupParent::OnCallReceived(
        const Message& msg__,
        Message*& reply__) -> PProxyConfigLookupParent::Result
{
    MOZ_ASSERT_UNREACHABLE("message protocol not supported");
    return MsgNotKnown;
}

auto PProxyConfigLookupParent::ClearSubtree() -> void
{
}



} // namespace net
} // namespace mozilla
namespace mozilla {
namespace ipc {
auto IPDLParamTraits<mozilla::net::PProxyConfigLookupParent*>::Write(
        IPC::Message* aMsg,
        mozilla::ipc::IProtocol* aActor,
        const paramType& aVar) -> void
{
    int32_t id;
    if (!aVar) {
        id = 0;  // kNullActorId
    } else {
        id = aVar->Id();
        if (id == 1) {  // kFreedActorId
            aVar->FatalError("Actor has been |delete|d");
        }
        MOZ_RELEASE_ASSERT(
            aActor->GetIPCChannel() == aVar->GetIPCChannel(),
            "Actor must be from the same channel as the"
            " actor it's being sent over");
        MOZ_RELEASE_ASSERT(
            aVar->CanSend(),
            "Actor must still be open when sending");
    }

    WriteIPDLParam(aMsg, aActor, id);
}

auto IPDLParamTraits<mozilla::net::PProxyConfigLookupParent*>::Read(
        const IPC::Message* aMsg,
        PickleIterator* aIter,
        mozilla::ipc::IProtocol* aActor,
        paramType* aVar) -> bool
{
    mozilla::Maybe<mozilla::ipc::IProtocol*> actor =
        aActor->ReadActor(aMsg, aIter, true, "PProxyConfigLookup", PProxyConfigLookupMsgStart);
    if (actor.isNothing()) {
        return false;
    }

    *aVar = static_cast<mozilla::net::PProxyConfigLookupParent*>(actor.value());
    return true;
}

} // namespace ipc
} // namespace mozilla
