//
// Automatically generated by ipdlc.
// Edit at your own risk
//


#include "mozilla/embedding/PPrintSettingsDialogChild.h"
#include "mozilla/ProfilerLabels.h"

#include "mozilla/embedding/PPrintingChild.h"
#include "mozilla/layout/PRemotePrintJobChild.h"

#include "ipc/IPCMessageUtils.h"
#include "nsIFile.h"
#include "mozilla/ipc/Endpoint.h"
#include "mozilla/ipc/ProtocolMessageUtils.h"
#include "mozilla/ipc/ProtocolUtils.h"
#include "mozilla/ipc/ShmemMessageUtils.h"
#include "mozilla/ipc/TaintingIPCUtils.h"

namespace mozilla {
namespace embedding {


MOZ_IMPLICIT PPrintSettingsDialogChild::PPrintSettingsDialogChild() :
    mozilla::ipc::IProtocol(PPrintSettingsDialogMsgStart, mozilla::ipc::ChildSide)
{
    MOZ_COUNT_CTOR(PPrintSettingsDialogChild);
}

PPrintSettingsDialogChild::~PPrintSettingsDialogChild()
{
    MOZ_COUNT_DTOR(PPrintSettingsDialogChild);
}

auto PPrintSettingsDialogChild::Manager() const -> PPrintingChild*
{
    return static_cast<PPrintingChild*>(IProtocol::Manager());
}

auto PPrintSettingsDialogChild::AllManagedActors(nsTArray<RefPtr<mozilla::ipc::ActorLifecycleProxy>>& arr__) const -> void
{
    uint32_t total = 0;
    arr__.SetCapacity(total);

}

auto PPrintSettingsDialogChild::RemoveManagee(
        int32_t aProtocolId,
        IProtocol* aListener) -> void
{
    FatalError("unreached");
    return;
}

auto PPrintSettingsDialogChild::DeallocManagee(
        int32_t aProtocolId,
        IProtocol* aListener) -> void
{
    FatalError("unreached");
    return;
}

auto PPrintSettingsDialogChild::OnMessageReceived(const Message& msg__) -> PPrintSettingsDialogChild::Result
{
    switch (msg__.type()) {
    case PPrintSettingsDialog::Msg___delete____ID:
        {
            if (mozilla::ipc::LoggingEnabledFor("PPrintSettingsDialogChild")) {
                mozilla::ipc::LogMessageForProtocol(
                    "PPrintSettingsDialogChild",
                    OtherPid(),
                    "Received ",
                    (&(msg__))->type(),
                    mozilla::ipc::MessageDirection::eReceiving);
            }
            AUTO_PROFILER_LABEL("PPrintSettingsDialog::Msg___delete__", OTHER);

            PickleIterator iter__{msg__};
            PPrintSettingsDialogChild* actor{};
            PrintDataOrNSResult result{};

            if ((!(ReadIPDLParam((&(msg__)), (&(iter__)), this, (&(actor)))))) {
                FatalError("Error deserializing 'PPrintSettingsDialog'");
                return MsgValueError;
            }
            if ((!((*((&(actor))))))) {
                FatalError("Error deserializing 'PPrintSettingsDialog'");
                return MsgValueError;
            }
            // Sentinel = 'actor'
            if ((!(((&(msg__)))->ReadSentinel((&(iter__)), 102892058)))) {
                mozilla::ipc::SentinelReadError("Error deserializing 'PPrintSettingsDialog'");
                return MsgValueError;
            }
            if ((!(ReadIPDLParam((&(msg__)), (&(iter__)), this, (&(result)))))) {
                FatalError("Error deserializing 'PrintDataOrNSResult'");
                return MsgValueError;
            }
            // Sentinel = 'result'
            if ((!(((&(msg__)))->ReadSentinel((&(iter__)), 153223840)))) {
                mozilla::ipc::SentinelReadError("Error deserializing 'PrintDataOrNSResult'");
                return MsgValueError;
            }
            msg__.EndRead(iter__, msg__.type());
            if ((!((this)->Recv__delete__(std::move(result))))) {
                mozilla::ipc::ProtocolErrorBreakpoint("Handler returned error code!");
                // Error handled in mozilla::ipc::IPCResult
                return MsgProcessingError;
            }


            IProtocol* mgr = actor->Manager();
            actor->DestroySubtree(Deletion);
            actor->ClearSubtree();
            mgr->RemoveManagee(PPrintSettingsDialogMsgStart, actor);

            return MsgProcessed;
        }
    default:
        return MsgNotKnown;
    }
}

auto PPrintSettingsDialogChild::OnMessageReceived(
        const Message& msg__,
        Message*& reply__) -> PPrintSettingsDialogChild::Result
{
    return MsgNotKnown;
}

auto PPrintSettingsDialogChild::OnCallReceived(
        const Message& msg__,
        Message*& reply__) -> PPrintSettingsDialogChild::Result
{
    MOZ_ASSERT_UNREACHABLE("message protocol not supported");
    return MsgNotKnown;
}

auto PPrintSettingsDialogChild::ClearSubtree() -> void
{
}



} // namespace embedding
} // namespace mozilla
namespace mozilla {
namespace ipc {
auto IPDLParamTraits<mozilla::embedding::PPrintSettingsDialogChild*>::Write(
        IPC::Message* aMsg,
        mozilla::ipc::IProtocol* aActor,
        const paramType& aVar) -> void
{
    int32_t id;
    if (!aVar) {
        id = 0;  // kNullActorId
    } else {
        id = aVar->Id();
        if (id == 1) {  // kFreedActorId
            aVar->FatalError("Actor has been |delete|d");
        }
        MOZ_RELEASE_ASSERT(
            aActor->GetIPCChannel() == aVar->GetIPCChannel(),
            "Actor must be from the same channel as the"
            " actor it's being sent over");
        MOZ_RELEASE_ASSERT(
            aVar->CanSend(),
            "Actor must still be open when sending");
    }

    WriteIPDLParam(aMsg, aActor, id);
}

auto IPDLParamTraits<mozilla::embedding::PPrintSettingsDialogChild*>::Read(
        const IPC::Message* aMsg,
        PickleIterator* aIter,
        mozilla::ipc::IProtocol* aActor,
        paramType* aVar) -> bool
{
    mozilla::Maybe<mozilla::ipc::IProtocol*> actor =
        aActor->ReadActor(aMsg, aIter, true, "PPrintSettingsDialog", PPrintSettingsDialogMsgStart);
    if (actor.isNothing()) {
        return false;
    }

    *aVar = static_cast<mozilla::embedding::PPrintSettingsDialogChild*>(actor.value());
    return true;
}

} // namespace ipc
} // namespace mozilla
