//
// Automatically generated by ipdlc.
// Edit at your own risk
//


#include "mozilla/dom/PEndpointForReportChild.h"
#include "mozilla/ProfilerLabels.h"
#include "mozilla/dom/EndpointForReportChild.h"

#include "mozilla/ipc/PBackgroundChild.h"

#include "ipc/IPCMessageUtils.h"
#include "nsIFile.h"
#include "mozilla/ipc/Endpoint.h"
#include "mozilla/ipc/ProtocolMessageUtils.h"
#include "mozilla/ipc/ProtocolUtils.h"
#include "mozilla/ipc/ShmemMessageUtils.h"
#include "mozilla/ipc/TaintingIPCUtils.h"

namespace mozilla {
namespace dom {


MOZ_IMPLICIT PEndpointForReportChild::PEndpointForReportChild() :
    mozilla::ipc::IProtocol(PEndpointForReportMsgStart, mozilla::ipc::ChildSide)
{
    MOZ_COUNT_CTOR(PEndpointForReportChild);
}

PEndpointForReportChild::~PEndpointForReportChild()
{
    MOZ_COUNT_DTOR(PEndpointForReportChild);
}

auto PEndpointForReportChild::Manager() const -> PBackgroundChild*
{
    return static_cast<PBackgroundChild*>(IProtocol::Manager());
}

auto PEndpointForReportChild::AllManagedActors(nsTArray<RefPtr<mozilla::ipc::ActorLifecycleProxy>>& arr__) const -> void
{
    uint32_t total = 0;
    arr__.SetCapacity(total);

}

auto PEndpointForReportChild::RemoveManagee(
        int32_t aProtocolId,
        IProtocol* aListener) -> void
{
    FatalError("unreached");
    return;
}

auto PEndpointForReportChild::DeallocManagee(
        int32_t aProtocolId,
        IProtocol* aListener) -> void
{
    FatalError("unreached");
    return;
}

auto PEndpointForReportChild::OnMessageReceived(const Message& msg__) -> PEndpointForReportChild::Result
{
    switch (msg__.type()) {
    case PEndpointForReport::Msg___delete____ID:
        {
            if (mozilla::ipc::LoggingEnabledFor("PEndpointForReportChild")) {
                mozilla::ipc::LogMessageForProtocol(
                    "PEndpointForReportChild",
                    OtherPid(),
                    "Received ",
                    (&(msg__))->type(),
                    mozilla::ipc::MessageDirection::eReceiving);
            }
            AUTO_PROFILER_LABEL("PEndpointForReport::Msg___delete__", OTHER);

            PickleIterator iter__{msg__};
            PEndpointForReportChild* actor{};
            nsCString endpointURL{};

            if ((!(ReadIPDLParam((&(msg__)), (&(iter__)), this, (&(actor)))))) {
                FatalError("Error deserializing 'PEndpointForReport'");
                return MsgValueError;
            }
            if ((!((*((&(actor))))))) {
                FatalError("Error deserializing 'PEndpointForReport'");
                return MsgValueError;
            }
            // Sentinel = 'actor'
            if ((!(((&(msg__)))->ReadSentinel((&(iter__)), 102892058)))) {
                mozilla::ipc::SentinelReadError("Error deserializing 'PEndpointForReport'");
                return MsgValueError;
            }
            if ((!(ReadIPDLParam((&(msg__)), (&(iter__)), this, (&(endpointURL)))))) {
                FatalError("Error deserializing 'nsCString'");
                return MsgValueError;
            }
            // Sentinel = 'endpointURL'
            if ((!(((&(msg__)))->ReadSentinel((&(iter__)), 454427733)))) {
                mozilla::ipc::SentinelReadError("Error deserializing 'nsCString'");
                return MsgValueError;
            }
            msg__.EndRead(iter__, msg__.type());
            if ((!((static_cast<EndpointForReportChild*>(this))->Recv__delete__(std::move(endpointURL))))) {
                mozilla::ipc::ProtocolErrorBreakpoint("Handler returned error code!");
                // Error handled in mozilla::ipc::IPCResult
                return MsgProcessingError;
            }


            IProtocol* mgr = actor->Manager();
            actor->DestroySubtree(Deletion);
            actor->ClearSubtree();
            mgr->RemoveManagee(PEndpointForReportMsgStart, actor);

            return MsgProcessed;
        }
    default:
        return MsgNotKnown;
    }
}

auto PEndpointForReportChild::OnMessageReceived(
        const Message& msg__,
        Message*& reply__) -> PEndpointForReportChild::Result
{
    return MsgNotKnown;
}

auto PEndpointForReportChild::OnCallReceived(
        const Message& msg__,
        Message*& reply__) -> PEndpointForReportChild::Result
{
    MOZ_ASSERT_UNREACHABLE("message protocol not supported");
    return MsgNotKnown;
}

auto PEndpointForReportChild::ClearSubtree() -> void
{
}



} // namespace dom
} // namespace mozilla
namespace mozilla {
namespace ipc {
auto IPDLParamTraits<mozilla::dom::PEndpointForReportChild*>::Write(
        IPC::Message* aMsg,
        mozilla::ipc::IProtocol* aActor,
        const paramType& aVar) -> void
{
    int32_t id;
    if (!aVar) {
        id = 0;  // kNullActorId
    } else {
        id = aVar->Id();
        if (id == 1) {  // kFreedActorId
            aVar->FatalError("Actor has been |delete|d");
        }
        MOZ_RELEASE_ASSERT(
            aActor->GetIPCChannel() == aVar->GetIPCChannel(),
            "Actor must be from the same channel as the"
            " actor it's being sent over");
        MOZ_RELEASE_ASSERT(
            aVar->CanSend(),
            "Actor must still be open when sending");
    }

    WriteIPDLParam(aMsg, aActor, id);
}

auto IPDLParamTraits<mozilla::dom::PEndpointForReportChild*>::Read(
        const IPC::Message* aMsg,
        PickleIterator* aIter,
        mozilla::ipc::IProtocol* aActor,
        paramType* aVar) -> bool
{
    mozilla::Maybe<mozilla::ipc::IProtocol*> actor =
        aActor->ReadActor(aMsg, aIter, true, "PEndpointForReport", PEndpointForReportMsgStart);
    if (actor.isNothing()) {
        return false;
    }

    *aVar = static_cast<mozilla::dom::PEndpointForReportChild*>(actor.value());
    return true;
}

} // namespace ipc
} // namespace mozilla
