//
// Automatically generated by ipdlc.
// Edit at your own risk
//


#include "mozilla/dom/PBackgroundSDBConnectionChild.h"
#include "mozilla/ProfilerLabels.h"

#include "mozilla/ipc/PBackgroundChild.h"
#include "mozilla/dom/PBackgroundSDBRequestChild.h"

#include "ipc/IPCMessageUtils.h"
#include "nsIFile.h"
#include "mozilla/ipc/Endpoint.h"
#include "mozilla/ipc/ProtocolMessageUtils.h"
#include "mozilla/ipc/ProtocolUtils.h"
#include "mozilla/ipc/ShmemMessageUtils.h"
#include "mozilla/ipc/TaintingIPCUtils.h"

namespace mozilla {
namespace dom {


auto PBackgroundSDBConnectionChild::Recv__delete__() -> mozilla::ipc::IPCResult
{
    return IPC_OK();
}

MOZ_IMPLICIT PBackgroundSDBConnectionChild::PBackgroundSDBConnectionChild() :
    mozilla::ipc::IProtocol(PBackgroundSDBConnectionMsgStart, mozilla::ipc::ChildSide)
{
    MOZ_COUNT_CTOR(PBackgroundSDBConnectionChild);
}

PBackgroundSDBConnectionChild::~PBackgroundSDBConnectionChild()
{
    MOZ_COUNT_DTOR(PBackgroundSDBConnectionChild);
}

auto PBackgroundSDBConnectionChild::Manager() const -> PBackgroundChild*
{
    return static_cast<PBackgroundChild*>(IProtocol::Manager());
}

auto PBackgroundSDBConnectionChild::ManagedPBackgroundSDBRequestChild(nsTArray<PBackgroundSDBRequestChild*>& aArr) const -> void
{
    mManagedPBackgroundSDBRequestChild.ToArray(aArr);
}

auto PBackgroundSDBConnectionChild::ManagedPBackgroundSDBRequestChild() const -> const ManagedContainer<PBackgroundSDBRequestChild>&
{
    return mManagedPBackgroundSDBRequestChild;
}

auto PBackgroundSDBConnectionChild::AllManagedActors(nsTArray<RefPtr<mozilla::ipc::ActorLifecycleProxy>>& arr__) const -> void
{
    uint32_t total = 0;
    total += mManagedPBackgroundSDBRequestChild.Count();
    arr__.SetCapacity(total);

    for (auto it = mManagedPBackgroundSDBRequestChild.ConstIter(); !it.Done(); it.Next()) {
        arr__.AppendElement(it.Get()->GetKey()->GetLifecycleProxy());
    }

}

auto PBackgroundSDBConnectionChild::OpenPBackgroundSDBRequestEndpoint(PBackgroundSDBRequestChild* aActor) -> ManagedEndpoint<PBackgroundSDBRequestParent>
{
    if (!aActor) {
        NS_WARNING("Cannot bind null PBackgroundSDBRequestChild actor");
        return ManagedEndpoint<PBackgroundSDBRequestParent>();
    }

    aActor->SetManagerAndRegister(this);
    mManagedPBackgroundSDBRequestChild.PutEntry(aActor);

    return ManagedEndpoint<PBackgroundSDBRequestParent>(mozilla::ipc::PrivateIPDLInterface(), aActor->Id());
}

auto PBackgroundSDBConnectionChild::BindPBackgroundSDBRequestEndpoint(
        ManagedEndpoint<PBackgroundSDBRequestChild> aEndpoint,
        PBackgroundSDBRequestChild* aActor) -> bool
{
    MOZ_RELEASE_ASSERT(aEndpoint.ActorId(), "Invalid Endpoint!");
    if (!aActor) {
        NS_WARNING("Cannot bind null PBackgroundSDBRequestChild actor");
        return false;
    }

    aActor->SetManagerAndRegister(this, *aEndpoint.ActorId());
    mManagedPBackgroundSDBRequestChild.PutEntry(aActor);

    return true;
}

auto PBackgroundSDBConnectionChild::SendDeleteMe() -> bool
{
    IPC::Message* msg__ = PBackgroundSDBConnection::Msg_DeleteMe(Id());





    if (mozilla::ipc::LoggingEnabledFor("PBackgroundSDBConnectionChild")) {
        mozilla::ipc::LogMessageForProtocol(
            "PBackgroundSDBConnectionChild",
            OtherPid(),
            "Sending ",
            msg__->type(),
            mozilla::ipc::MessageDirection::eSending);
    }
    AUTO_PROFILER_LABEL("PBackgroundSDBConnection::Msg_DeleteMe", OTHER);

    bool sendok__ = ChannelSend(msg__);
    return sendok__;
}

auto PBackgroundSDBConnectionChild::SendPBackgroundSDBRequestConstructor(const SDBRequestParams& params) -> PBackgroundSDBRequestChild*
{
    PBackgroundSDBRequestChild* actor = (this)->AllocPBackgroundSDBRequestChild(params);
    return SendPBackgroundSDBRequestConstructor(actor, params);
}

auto PBackgroundSDBConnectionChild::SendPBackgroundSDBRequestConstructor(
        PBackgroundSDBRequestChild* actor,
        const SDBRequestParams& params) -> PBackgroundSDBRequestChild*
{
    if (!actor) {
        NS_WARNING("Cannot bind null PBackgroundSDBRequestChild actor");
        return nullptr;
    }

    actor->SetManagerAndRegister(this);
    mManagedPBackgroundSDBRequestChild.PutEntry(actor);


    // Build our constructor message.
    IPC::Message* msg__ = PBackgroundSDBConnection::Msg_PBackgroundSDBRequestConstructor(Id());

    MOZ_RELEASE_ASSERT(actor, "NULL actor value passed to non-nullable param");
    WriteIPDLParam(msg__, this, actor);
    // Sentinel = 'actor'
    (msg__)->WriteSentinel(102892058);
    WriteIPDLParam(msg__, this, params);
    // Sentinel = 'params'
    (msg__)->WriteSentinel(146997893);




    // Notify the other side about the newly created actor. This can
    // fail if our manager has already been destroyed.
    //
    // NOTE: If the send call fails due to toplevel channel teardown,
    // the `IProtocol::ChannelSend` wrapper absorbs the error for us,
    // so we don't tear down actors unexpectedly.

    if (mozilla::ipc::LoggingEnabledFor("PBackgroundSDBConnectionChild")) {
        mozilla::ipc::LogMessageForProtocol(
            "PBackgroundSDBConnectionChild",
            OtherPid(),
            "Sending ",
            msg__->type(),
            mozilla::ipc::MessageDirection::eSending);
    }
    AUTO_PROFILER_LABEL("PBackgroundSDBConnection::Msg_PBackgroundSDBRequestConstructor", OTHER);

    bool sendok__ = ChannelSend(msg__);


    // Warn, destroy the actor, and return null if the message failed to
    // send. Otherwise, return the successfully created actor reference.
    if (!sendok__) {
        NS_WARNING("Error sending PBackgroundSDBRequestChild constructor");
        IProtocol* mgr = actor->Manager();
        actor->DestroySubtree(FailedConstructor);
        actor->ClearSubtree();
        mgr->RemoveManagee(PBackgroundSDBRequestMsgStart, actor);

        return nullptr;
    }
    return actor;
}

auto PBackgroundSDBConnectionChild::RemoveManagee(
        int32_t aProtocolId,
        IProtocol* aListener) -> void
{
    switch (aProtocolId) {
    case PBackgroundSDBRequestMsgStart:
{
            PBackgroundSDBRequestChild* actor = static_cast<PBackgroundSDBRequestChild*>(aListener);
            auto& container = mManagedPBackgroundSDBRequestChild;

            // Use a temporary variable here so all the assertion expressions
            // in the MOZ_RELEASE_ASSERT call below are textually identical;
            // the linker can then merge the strings from the assertion macro(s).
            MOZ_RELEASE_ASSERT(container.Contains(actor),
                "actor not managed by this!");
            container.RemoveEntry(actor);

            auto* proxy = actor->GetLifecycleProxy();
            NS_IF_RELEASE(proxy);
            return;
        }    default:
FatalError("unreached");
        return;    }
}

auto PBackgroundSDBConnectionChild::DeallocManagee(
        int32_t aProtocolId,
        IProtocol* aListener) -> void
{
    switch (aProtocolId) {
    case PBackgroundSDBRequestMsgStart:
        this->DeallocPBackgroundSDBRequestChild(static_cast<PBackgroundSDBRequestChild*>(aListener));
        return;
    default:
        FatalError("unreached");
        return;
    }
}

auto PBackgroundSDBConnectionChild::OnMessageReceived(const Message& msg__) -> PBackgroundSDBConnectionChild::Result
{
    switch (msg__.type()) {
    case PBackgroundSDBConnection::Reply_PBackgroundSDBRequestConstructor__ID:
        {
            return MsgProcessed;
        }
    case PBackgroundSDBConnection::Msg___delete____ID:
        {
            if (mozilla::ipc::LoggingEnabledFor("PBackgroundSDBConnectionChild")) {
                mozilla::ipc::LogMessageForProtocol(
                    "PBackgroundSDBConnectionChild",
                    OtherPid(),
                    "Received ",
                    (&(msg__))->type(),
                    mozilla::ipc::MessageDirection::eReceiving);
            }
            AUTO_PROFILER_LABEL("PBackgroundSDBConnection::Msg___delete__", OTHER);

            PickleIterator iter__{msg__};
            PBackgroundSDBConnectionChild* actor{};

            if ((!(ReadIPDLParam((&(msg__)), (&(iter__)), this, (&(actor)))))) {
                FatalError("Error deserializing 'PBackgroundSDBConnection'");
                return MsgValueError;
            }
            if ((!((*((&(actor))))))) {
                FatalError("Error deserializing 'PBackgroundSDBConnection'");
                return MsgValueError;
            }
            // Sentinel = 'actor'
            if ((!(((&(msg__)))->ReadSentinel((&(iter__)), 102892058)))) {
                mozilla::ipc::SentinelReadError("Error deserializing 'PBackgroundSDBConnection'");
                return MsgValueError;
            }
            msg__.EndRead(iter__, msg__.type());
            if ((!((this)->Recv__delete__()))) {
                mozilla::ipc::ProtocolErrorBreakpoint("Handler returned error code!");
                // Error handled in mozilla::ipc::IPCResult
                return MsgProcessingError;
            }


            IProtocol* mgr = actor->Manager();
            actor->DestroySubtree(Deletion);
            actor->ClearSubtree();
            mgr->RemoveManagee(PBackgroundSDBConnectionMsgStart, actor);

            return MsgProcessed;
        }
    case PBackgroundSDBConnection::Msg_AllowToClose__ID:
        {
            if (mozilla::ipc::LoggingEnabledFor("PBackgroundSDBConnectionChild")) {
                mozilla::ipc::LogMessageForProtocol(
                    "PBackgroundSDBConnectionChild",
                    OtherPid(),
                    "Received ",
                    (&(msg__))->type(),
                    mozilla::ipc::MessageDirection::eReceiving);
            }
            AUTO_PROFILER_LABEL("PBackgroundSDBConnection::Msg_AllowToClose", OTHER);

            if ((!((this)->RecvAllowToClose()))) {
                mozilla::ipc::ProtocolErrorBreakpoint("Handler returned error code!");
                // Error handled in mozilla::ipc::IPCResult
                return MsgProcessingError;
            }

            return MsgProcessed;
        }
    case PBackgroundSDBConnection::Msg_Closed__ID:
        {
            if (mozilla::ipc::LoggingEnabledFor("PBackgroundSDBConnectionChild")) {
                mozilla::ipc::LogMessageForProtocol(
                    "PBackgroundSDBConnectionChild",
                    OtherPid(),
                    "Received ",
                    (&(msg__))->type(),
                    mozilla::ipc::MessageDirection::eReceiving);
            }
            AUTO_PROFILER_LABEL("PBackgroundSDBConnection::Msg_Closed", OTHER);

            if ((!((this)->RecvClosed()))) {
                mozilla::ipc::ProtocolErrorBreakpoint("Handler returned error code!");
                // Error handled in mozilla::ipc::IPCResult
                return MsgProcessingError;
            }

            return MsgProcessed;
        }
    default:
        return MsgNotKnown;
    }
}

auto PBackgroundSDBConnectionChild::OnMessageReceived(
        const Message& msg__,
        Message*& reply__) -> PBackgroundSDBConnectionChild::Result
{
    return MsgNotKnown;
}

auto PBackgroundSDBConnectionChild::OnCallReceived(
        const Message& msg__,
        Message*& reply__) -> PBackgroundSDBConnectionChild::Result
{
    MOZ_ASSERT_UNREACHABLE("message protocol not supported");
    return MsgNotKnown;
}

auto PBackgroundSDBConnectionChild::ClearSubtree() -> void
{
    for (auto it = mManagedPBackgroundSDBRequestChild.Iter(); !it.Done(); it.Next()) {
        it.Get()->GetKey()->ClearSubtree();
    }
    for (auto it = mManagedPBackgroundSDBRequestChild.Iter(); !it.Done(); it.Next()) {
        // Recursively releasing mManagedPBackgroundSDBRequestChild kids.
        auto* proxy = it.Get()->GetKey()->GetLifecycleProxy();
        NS_IF_RELEASE(proxy);
    }
    mManagedPBackgroundSDBRequestChild.Clear();

}



} // namespace dom
} // namespace mozilla
namespace mozilla {
namespace ipc {
auto IPDLParamTraits<mozilla::dom::PBackgroundSDBConnectionChild*>::Write(
        IPC::Message* aMsg,
        mozilla::ipc::IProtocol* aActor,
        const paramType& aVar) -> void
{
    int32_t id;
    if (!aVar) {
        id = 0;  // kNullActorId
    } else {
        id = aVar->Id();
        if (id == 1) {  // kFreedActorId
            aVar->FatalError("Actor has been |delete|d");
        }
        MOZ_RELEASE_ASSERT(
            aActor->GetIPCChannel() == aVar->GetIPCChannel(),
            "Actor must be from the same channel as the"
            " actor it's being sent over");
        MOZ_RELEASE_ASSERT(
            aVar->CanSend(),
            "Actor must still be open when sending");
    }

    WriteIPDLParam(aMsg, aActor, id);
}

auto IPDLParamTraits<mozilla::dom::PBackgroundSDBConnectionChild*>::Read(
        const IPC::Message* aMsg,
        PickleIterator* aIter,
        mozilla::ipc::IProtocol* aActor,
        paramType* aVar) -> bool
{
    mozilla::Maybe<mozilla::ipc::IProtocol*> actor =
        aActor->ReadActor(aMsg, aIter, true, "PBackgroundSDBConnection", PBackgroundSDBConnectionMsgStart);
    if (actor.isNothing()) {
        return false;
    }

    *aVar = static_cast<mozilla::dom::PBackgroundSDBConnectionChild*>(actor.value());
    return true;
}

} // namespace ipc
} // namespace mozilla
