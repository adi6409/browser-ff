//
// Automatically generated by ipdlc.
// Edit at your own risk
//


#include "mozilla/dom/quota/PQuotaRequestChild.h"
#include "mozilla/ProfilerLabels.h"

#include "mozilla/dom/quota/PQuotaChild.h"

#include "ipc/IPCMessageUtils.h"
#include "nsIFile.h"
#include "mozilla/ipc/Endpoint.h"
#include "mozilla/ipc/ProtocolMessageUtils.h"
#include "mozilla/ipc/ProtocolUtils.h"
#include "mozilla/ipc/ShmemMessageUtils.h"
#include "mozilla/ipc/TaintingIPCUtils.h"

namespace mozilla {
namespace dom {
namespace quota {


MOZ_IMPLICIT PQuotaRequestChild::PQuotaRequestChild() :
    mozilla::ipc::IProtocol(PQuotaRequestMsgStart, mozilla::ipc::ChildSide)
{
    MOZ_COUNT_CTOR(PQuotaRequestChild);
}

PQuotaRequestChild::~PQuotaRequestChild()
{
    MOZ_COUNT_DTOR(PQuotaRequestChild);
}

auto PQuotaRequestChild::Manager() const -> PQuotaChild*
{
    return static_cast<PQuotaChild*>(IProtocol::Manager());
}

auto PQuotaRequestChild::AllManagedActors(nsTArray<RefPtr<mozilla::ipc::ActorLifecycleProxy>>& arr__) const -> void
{
    uint32_t total = 0;
    arr__.SetCapacity(total);

}

auto PQuotaRequestChild::RemoveManagee(
        int32_t aProtocolId,
        IProtocol* aListener) -> void
{
    FatalError("unreached");
    return;
}

auto PQuotaRequestChild::DeallocManagee(
        int32_t aProtocolId,
        IProtocol* aListener) -> void
{
    FatalError("unreached");
    return;
}

auto PQuotaRequestChild::OnMessageReceived(const Message& msg__) -> PQuotaRequestChild::Result
{
    switch (msg__.type()) {
    case PQuotaRequest::Msg___delete____ID:
        {
            if (mozilla::ipc::LoggingEnabledFor("PQuotaRequestChild")) {
                mozilla::ipc::LogMessageForProtocol(
                    "PQuotaRequestChild",
                    OtherPid(),
                    "Received ",
                    (&(msg__))->type(),
                    mozilla::ipc::MessageDirection::eReceiving);
            }
            AUTO_PROFILER_LABEL("PQuotaRequest::Msg___delete__", OTHER);

            PickleIterator iter__{msg__};
            PQuotaRequestChild* actor{};
            RequestResponse response{};

            if ((!(ReadIPDLParam((&(msg__)), (&(iter__)), this, (&(actor)))))) {
                FatalError("Error deserializing 'PQuotaRequest'");
                return MsgValueError;
            }
            if ((!((*((&(actor))))))) {
                FatalError("Error deserializing 'PQuotaRequest'");
                return MsgValueError;
            }
            // Sentinel = 'actor'
            if ((!(((&(msg__)))->ReadSentinel((&(iter__)), 102892058)))) {
                mozilla::ipc::SentinelReadError("Error deserializing 'PQuotaRequest'");
                return MsgValueError;
            }
            if ((!(ReadIPDLParam((&(msg__)), (&(iter__)), this, (&(response)))))) {
                FatalError("Error deserializing 'RequestResponse'");
                return MsgValueError;
            }
            // Sentinel = 'response'
            if ((!(((&(msg__)))->ReadSentinel((&(iter__)), 260965232)))) {
                mozilla::ipc::SentinelReadError("Error deserializing 'RequestResponse'");
                return MsgValueError;
            }
            msg__.EndRead(iter__, msg__.type());
            if ((!((this)->Recv__delete__(std::move(response))))) {
                mozilla::ipc::ProtocolErrorBreakpoint("Handler returned error code!");
                // Error handled in mozilla::ipc::IPCResult
                return MsgProcessingError;
            }


            IProtocol* mgr = actor->Manager();
            actor->DestroySubtree(Deletion);
            actor->ClearSubtree();
            mgr->RemoveManagee(PQuotaRequestMsgStart, actor);

            return MsgProcessed;
        }
    default:
        return MsgNotKnown;
    }
}

auto PQuotaRequestChild::OnMessageReceived(
        const Message& msg__,
        Message*& reply__) -> PQuotaRequestChild::Result
{
    return MsgNotKnown;
}

auto PQuotaRequestChild::OnCallReceived(
        const Message& msg__,
        Message*& reply__) -> PQuotaRequestChild::Result
{
    MOZ_ASSERT_UNREACHABLE("message protocol not supported");
    return MsgNotKnown;
}

auto PQuotaRequestChild::ClearSubtree() -> void
{
}



} // namespace quota
} // namespace dom
} // namespace mozilla
namespace mozilla {
namespace ipc {
auto IPDLParamTraits<mozilla::dom::quota::PQuotaRequestChild*>::Write(
        IPC::Message* aMsg,
        mozilla::ipc::IProtocol* aActor,
        const paramType& aVar) -> void
{
    int32_t id;
    if (!aVar) {
        id = 0;  // kNullActorId
    } else {
        id = aVar->Id();
        if (id == 1) {  // kFreedActorId
            aVar->FatalError("Actor has been |delete|d");
        }
        MOZ_RELEASE_ASSERT(
            aActor->GetIPCChannel() == aVar->GetIPCChannel(),
            "Actor must be from the same channel as the"
            " actor it's being sent over");
        MOZ_RELEASE_ASSERT(
            aVar->CanSend(),
            "Actor must still be open when sending");
    }

    WriteIPDLParam(aMsg, aActor, id);
}

auto IPDLParamTraits<mozilla::dom::quota::PQuotaRequestChild*>::Read(
        const IPC::Message* aMsg,
        PickleIterator* aIter,
        mozilla::ipc::IProtocol* aActor,
        paramType* aVar) -> bool
{
    mozilla::Maybe<mozilla::ipc::IProtocol*> actor =
        aActor->ReadActor(aMsg, aIter, true, "PQuotaRequest", PQuotaRequestMsgStart);
    if (actor.isNothing()) {
        return false;
    }

    *aVar = static_cast<mozilla::dom::quota::PQuotaRequestChild*>(actor.value());
    return true;
}

} // namespace ipc
} // namespace mozilla
