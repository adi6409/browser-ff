//
// Automatically generated by ipdlc.
// Edit at your own risk
//


#include "mozilla/dom/PVsyncChild.h"
#include "mozilla/ProfilerLabels.h"
#include "mozilla/TimeStamp.h"
#include "mozilla/dom/VsyncChild.h"
#include "mozilla/layers/LayersMessageUtils.h"

#include "mozilla/ipc/PBackgroundChild.h"
#include "mozilla/dom/PBrowserChild.h"

#include "ipc/IPCMessageUtils.h"
#include "nsIFile.h"
#include "mozilla/ipc/Endpoint.h"
#include "mozilla/ipc/ProtocolMessageUtils.h"
#include "mozilla/ipc/ProtocolUtils.h"
#include "mozilla/ipc/ShmemMessageUtils.h"
#include "mozilla/ipc/TaintingIPCUtils.h"

namespace mozilla {
namespace dom {


MOZ_IMPLICIT PVsyncChild::PVsyncChild() :
    mozilla::ipc::IProtocol(PVsyncMsgStart, mozilla::ipc::ChildSide)
{
    MOZ_COUNT_CTOR(PVsyncChild);
}

PVsyncChild::~PVsyncChild()
{
    MOZ_COUNT_DTOR(PVsyncChild);
}

auto PVsyncChild::AllManagedActors(nsTArray<RefPtr<mozilla::ipc::ActorLifecycleProxy>>& arr__) const -> void
{
    uint32_t total = 0;
    arr__.SetCapacity(total);

}

auto PVsyncChild::SendObserve() -> bool
{
    IPC::Message* msg__ = PVsync::Msg_Observe(Id());





    if (mozilla::ipc::LoggingEnabledFor("PVsyncChild")) {
        mozilla::ipc::LogMessageForProtocol(
            "PVsyncChild",
            OtherPid(),
            "Sending ",
            msg__->type(),
            mozilla::ipc::MessageDirection::eSending);
    }
    AUTO_PROFILER_LABEL("PVsync::Msg_Observe", OTHER);

    bool sendok__ = ChannelSend(msg__);
    return sendok__;
}

auto PVsyncChild::SendUnobserve() -> bool
{
    IPC::Message* msg__ = PVsync::Msg_Unobserve(Id());





    if (mozilla::ipc::LoggingEnabledFor("PVsyncChild")) {
        mozilla::ipc::LogMessageForProtocol(
            "PVsyncChild",
            OtherPid(),
            "Sending ",
            msg__->type(),
            mozilla::ipc::MessageDirection::eSending);
    }
    AUTO_PROFILER_LABEL("PVsync::Msg_Unobserve", OTHER);

    bool sendok__ = ChannelSend(msg__);
    return sendok__;
}

auto PVsyncChild::Send__delete__(PVsyncChild* actor) -> bool
{
    if (!actor || !actor->CanSend()) {
        NS_WARNING("Attempt to __delete__ missing or closed actor");
        return false;
    }
    IPC::Message* msg__ = PVsync::Msg___delete__((actor)->Id());

    MOZ_RELEASE_ASSERT(actor, "NULL actor value passed to non-nullable param");
    WriteIPDLParam(msg__, actor, actor);
    // Sentinel = 'actor'
    (msg__)->WriteSentinel(102892058);



    if (mozilla::ipc::LoggingEnabledFor("PVsyncChild")) {
        mozilla::ipc::LogMessageForProtocol(
            "PVsyncChild",
            (actor)->OtherPid(),
            "Sending ",
            msg__->type(),
            mozilla::ipc::MessageDirection::eSending);
    }
    AUTO_PROFILER_LABEL("PVsync::Msg___delete__", OTHER);

    bool sendok__ = (actor)->ChannelSend(msg__);

    IProtocol* mgr = actor->Manager();
    actor->DestroySubtree(Deletion);
    actor->ClearSubtree();
    mgr->RemoveManagee(PVsyncMsgStart, actor);
    return sendok__;
}

auto PVsyncChild::RemoveManagee(
        int32_t aProtocolId,
        IProtocol* aListener) -> void
{
    FatalError("unreached");
    return;
}

auto PVsyncChild::DeallocManagee(
        int32_t aProtocolId,
        IProtocol* aListener) -> void
{
    FatalError("unreached");
    return;
}

auto PVsyncChild::OnMessageReceived(const Message& msg__) -> PVsyncChild::Result
{
    switch (msg__.type()) {
    case PVsync::Msg_Notify__ID:
        {
            if (mozilla::ipc::LoggingEnabledFor("PVsyncChild")) {
                mozilla::ipc::LogMessageForProtocol(
                    "PVsyncChild",
                    OtherPid(),
                    "Received ",
                    (&(msg__))->type(),
                    mozilla::ipc::MessageDirection::eReceiving);
            }
            AUTO_PROFILER_LABEL("PVsync::Msg_Notify", OTHER);

            PickleIterator iter__{msg__};
            VsyncEvent aVsync{};
            float aVsyncRate{};

            if ((!(ReadIPDLParam((&(msg__)), (&(iter__)), this, (&(aVsync)))))) {
                FatalError("Error deserializing 'VsyncEvent'");
                return MsgValueError;
            }
            // Sentinel = 'aVsync'
            if ((!(((&(msg__)))->ReadSentinel((&(iter__)), 141558389)))) {
                mozilla::ipc::SentinelReadError("Error deserializing 'VsyncEvent'");
                return MsgValueError;
            }
            if ((!(ReadIPDLParam((&(msg__)), (&(iter__)), this, (&(aVsyncRate)))))) {
                FatalError("Error deserializing 'float'");
                return MsgValueError;
            }
            // Sentinel = 'aVsyncRate'
            if ((!(((&(msg__)))->ReadSentinel((&(iter__)), 368837633)))) {
                mozilla::ipc::SentinelReadError("Error deserializing 'float'");
                return MsgValueError;
            }
            msg__.EndRead(iter__, msg__.type());
            if ((!((static_cast<VsyncChild*>(this))->RecvNotify(std::move(aVsync), std::move(aVsyncRate))))) {
                mozilla::ipc::ProtocolErrorBreakpoint("Handler returned error code!");
                // Error handled in mozilla::ipc::IPCResult
                return MsgProcessingError;
            }

            return MsgProcessed;
        }
    case PVsync::Reply___delete____ID:
        {
            return MsgProcessed;
        }
    default:
        return MsgNotKnown;
    }
}

auto PVsyncChild::OnMessageReceived(
        const Message& msg__,
        Message*& reply__) -> PVsyncChild::Result
{
    return MsgNotKnown;
}

auto PVsyncChild::OnCallReceived(
        const Message& msg__,
        Message*& reply__) -> PVsyncChild::Result
{
    MOZ_ASSERT_UNREACHABLE("message protocol not supported");
    return MsgNotKnown;
}

auto PVsyncChild::ClearSubtree() -> void
{
}



} // namespace dom
} // namespace mozilla
namespace mozilla {
namespace ipc {
auto IPDLParamTraits<mozilla::dom::PVsyncChild*>::Write(
        IPC::Message* aMsg,
        mozilla::ipc::IProtocol* aActor,
        const paramType& aVar) -> void
{
    int32_t id;
    if (!aVar) {
        id = 0;  // kNullActorId
    } else {
        id = aVar->Id();
        if (id == 1) {  // kFreedActorId
            aVar->FatalError("Actor has been |delete|d");
        }
        MOZ_RELEASE_ASSERT(
            aActor->GetIPCChannel() == aVar->GetIPCChannel(),
            "Actor must be from the same channel as the"
            " actor it's being sent over");
        MOZ_RELEASE_ASSERT(
            aVar->CanSend(),
            "Actor must still be open when sending");
    }

    WriteIPDLParam(aMsg, aActor, id);
}

auto IPDLParamTraits<mozilla::dom::PVsyncChild*>::Read(
        const IPC::Message* aMsg,
        PickleIterator* aIter,
        mozilla::ipc::IProtocol* aActor,
        paramType* aVar) -> bool
{
    mozilla::Maybe<mozilla::ipc::IProtocol*> actor =
        aActor->ReadActor(aMsg, aIter, true, "PVsync", PVsyncMsgStart);
    if (actor.isNothing()) {
        return false;
    }

    *aVar = static_cast<mozilla::dom::PVsyncChild*>(actor.value());
    return true;
}

} // namespace ipc
} // namespace mozilla
