//
// Automatically generated by ipdlc.
// Edit at your own risk
//


#include "mozilla/layers/PCanvasChild.h"
#include "mozilla/ProfilerLabels.h"
#include "mozilla/layers/CanvasChild.h"
#include "mozilla/layers/LayersMessageUtils.h"


#include "ipc/IPCMessageUtils.h"
#include "nsIFile.h"
#include "mozilla/ipc/Endpoint.h"
#include "mozilla/ipc/ProtocolMessageUtils.h"
#include "mozilla/ipc/ProtocolUtils.h"
#include "mozilla/ipc/ShmemMessageUtils.h"
#include "mozilla/ipc/TaintingIPCUtils.h"

namespace mozilla {
namespace layers {


auto PCanvasChild::ProcessingError(
        Result aCode,
        const char* aReason) -> void
{
}

auto PCanvasChild::ShouldContinueFromReplyTimeout() -> bool
{
    return true;
}

auto PCanvasChild::EnteredCxxStack() -> void
{
}

auto PCanvasChild::ExitedCxxStack() -> void
{
}

auto PCanvasChild::EnteredCall() -> void
{
}

auto PCanvasChild::ExitedCall() -> void
{
}

MOZ_IMPLICIT PCanvasChild::PCanvasChild() :
    mozilla::ipc::IToplevelProtocol("PCanvasChild", PCanvasMsgStart, mozilla::ipc::ChildSide)
{
    MOZ_COUNT_CTOR(PCanvasChild);
}

PCanvasChild::~PCanvasChild()
{
    MOZ_COUNT_DTOR(PCanvasChild);
}

auto PCanvasChild::AllManagedActors(nsTArray<RefPtr<mozilla::ipc::ActorLifecycleProxy>>& arr__) const -> void
{
    uint32_t total = 0;
    arr__.SetCapacity(total);

}

auto PCanvasChild::SendInitTranslator(
        const TextureType& aTextureType,
        const Handle& aReadHandle,
        const CrossProcessSemaphoreHandle& aReaderSem,
        const CrossProcessSemaphoreHandle& aWriterSem) -> bool
{
    IPC::Message* msg__ = PCanvas::Msg_InitTranslator(MSG_ROUTING_CONTROL);

    WriteIPDLParam(msg__, this, aTextureType);
    // Sentinel = 'aTextureType'
    (msg__)->WriteSentinel(534119669);
    WriteIPDLParam(msg__, this, aReadHandle);
    // Sentinel = 'aReadHandle'
    (msg__)->WriteSentinel(410059818);
    WriteIPDLParam(msg__, this, aReaderSem);
    // Sentinel = 'aReaderSem'
    (msg__)->WriteSentinel(349045722);
    WriteIPDLParam(msg__, this, aWriterSem);
    // Sentinel = 'aWriterSem'
    (msg__)->WriteSentinel(368772100);




    if (mozilla::ipc::LoggingEnabledFor("PCanvasChild")) {
        mozilla::ipc::LogMessageForProtocol(
            "PCanvasChild",
            OtherPid(),
            "Sending ",
            msg__->type(),
            mozilla::ipc::MessageDirection::eSending);
    }
    AUTO_PROFILER_LABEL("PCanvas::Msg_InitTranslator", OTHER);

    bool sendok__ = ChannelSend(msg__);
    return sendok__;
}

auto PCanvasChild::SendResumeTranslation() -> bool
{
    IPC::Message* msg__ = PCanvas::Msg_ResumeTranslation(MSG_ROUTING_CONTROL);





    if (mozilla::ipc::LoggingEnabledFor("PCanvasChild")) {
        mozilla::ipc::LogMessageForProtocol(
            "PCanvasChild",
            OtherPid(),
            "Sending ",
            msg__->type(),
            mozilla::ipc::MessageDirection::eSending);
    }
    AUTO_PROFILER_LABEL("PCanvas::Msg_ResumeTranslation", OTHER);

    bool sendok__ = ChannelSend(msg__);
    return sendok__;
}

auto PCanvasChild::RemoveManagee(
        int32_t aProtocolId,
        IProtocol* aListener) -> void
{
    FatalError("unreached");
    return;
}

auto PCanvasChild::DeallocManagee(
        int32_t aProtocolId,
        IProtocol* aListener) -> void
{
    FatalError("unreached");
    return;
}

auto PCanvasChild::OnMessageReceived(const Message& msg__) -> PCanvasChild::Result
{
    switch (msg__.type()) {
    case PCanvas::Msg_NotifyDeviceChanged__ID:
        {
            if (mozilla::ipc::LoggingEnabledFor("PCanvasChild")) {
                mozilla::ipc::LogMessageForProtocol(
                    "PCanvasChild",
                    OtherPid(),
                    "Received ",
                    (&(msg__))->type(),
                    mozilla::ipc::MessageDirection::eReceiving);
            }
            AUTO_PROFILER_LABEL("PCanvas::Msg_NotifyDeviceChanged", OTHER);

            if ((!((static_cast<CanvasChild*>(this))->RecvNotifyDeviceChanged()))) {
                mozilla::ipc::ProtocolErrorBreakpoint("Handler returned error code!");
                // Error handled in mozilla::ipc::IPCResult
                return MsgProcessingError;
            }

            return MsgProcessed;
        }
    case PCanvas::Msg_Deactivate__ID:
        {
            if (mozilla::ipc::LoggingEnabledFor("PCanvasChild")) {
                mozilla::ipc::LogMessageForProtocol(
                    "PCanvasChild",
                    OtherPid(),
                    "Received ",
                    (&(msg__))->type(),
                    mozilla::ipc::MessageDirection::eReceiving);
            }
            AUTO_PROFILER_LABEL("PCanvas::Msg_Deactivate", OTHER);

            if ((!((static_cast<CanvasChild*>(this))->RecvDeactivate()))) {
                mozilla::ipc::ProtocolErrorBreakpoint("Handler returned error code!");
                // Error handled in mozilla::ipc::IPCResult
                return MsgProcessingError;
            }

            return MsgProcessed;
        }
    default:
        return MsgNotKnown;
    case SHMEM_CREATED_MESSAGE_TYPE:
        {
            FatalError("this protocol tree does not use shmem");
            return MsgNotKnown;
        }
    case SHMEM_DESTROYED_MESSAGE_TYPE:
        {
            FatalError("this protocol tree does not use shmem");
            return MsgNotKnown;
        }
    }
}

auto PCanvasChild::OnMessageReceived(
        const Message& msg__,
        Message*& reply__) -> PCanvasChild::Result
{
    MOZ_ASSERT_UNREACHABLE("message protocol not supported");
    return MsgNotKnown;
}

auto PCanvasChild::OnCallReceived(
        const Message& msg__,
        Message*& reply__) -> PCanvasChild::Result
{
    MOZ_ASSERT_UNREACHABLE("message protocol not supported");
    return MsgNotKnown;
}

auto PCanvasChild::OnChannelClose() -> void
{
    DestroySubtree(NormalShutdown);
    ClearSubtree();
    DeallocShmems();
    if (GetLifecycleProxy()) {
        GetLifecycleProxy()->Release();
    }
}

auto PCanvasChild::OnChannelError() -> void
{
    DestroySubtree(AbnormalShutdown);
    ClearSubtree();
    DeallocShmems();
    if (GetLifecycleProxy()) {
        GetLifecycleProxy()->Release();
    }
}

auto PCanvasChild::ClearSubtree() -> void
{
}



} // namespace layers
} // namespace mozilla
namespace mozilla {
namespace ipc {
auto IPDLParamTraits<mozilla::layers::PCanvasChild*>::Write(
        IPC::Message* aMsg,
        mozilla::ipc::IProtocol* aActor,
        const paramType& aVar) -> void
{
    int32_t id;
    if (!aVar) {
        id = 0;  // kNullActorId
    } else {
        id = aVar->Id();
        if (id == 1) {  // kFreedActorId
            aVar->FatalError("Actor has been |delete|d");
        }
        MOZ_RELEASE_ASSERT(
            aActor->GetIPCChannel() == aVar->GetIPCChannel(),
            "Actor must be from the same channel as the"
            " actor it's being sent over");
        MOZ_RELEASE_ASSERT(
            aVar->CanSend(),
            "Actor must still be open when sending");
    }

    WriteIPDLParam(aMsg, aActor, id);
}

auto IPDLParamTraits<mozilla::layers::PCanvasChild*>::Read(
        const IPC::Message* aMsg,
        PickleIterator* aIter,
        mozilla::ipc::IProtocol* aActor,
        paramType* aVar) -> bool
{
    mozilla::Maybe<mozilla::ipc::IProtocol*> actor =
        aActor->ReadActor(aMsg, aIter, true, "PCanvas", PCanvasMsgStart);
    if (actor.isNothing()) {
        return false;
    }

    *aVar = static_cast<mozilla::layers::PCanvasChild*>(actor.value());
    return true;
}

} // namespace ipc
} // namespace mozilla
