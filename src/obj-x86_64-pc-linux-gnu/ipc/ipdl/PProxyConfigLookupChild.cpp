//
// Automatically generated by ipdlc.
// Edit at your own risk
//


#include "mozilla/net/PProxyConfigLookupChild.h"
#include "mozilla/ProfilerLabels.h"
#include "mozilla/net/ProxyConfigLookupChild.h"

#include "mozilla/net/PSocketProcessChild.h"

#include "ipc/IPCMessageUtils.h"
#include "nsIFile.h"
#include "mozilla/ipc/Endpoint.h"
#include "mozilla/ipc/ProtocolMessageUtils.h"
#include "mozilla/ipc/ProtocolUtils.h"
#include "mozilla/ipc/ShmemMessageUtils.h"
#include "mozilla/ipc/TaintingIPCUtils.h"

namespace mozilla {
namespace net {


MOZ_IMPLICIT PProxyConfigLookupChild::PProxyConfigLookupChild() :
    mozilla::ipc::IProtocol(PProxyConfigLookupMsgStart, mozilla::ipc::ChildSide)
{
    MOZ_COUNT_CTOR(PProxyConfigLookupChild);
}

PProxyConfigLookupChild::~PProxyConfigLookupChild()
{
    MOZ_COUNT_DTOR(PProxyConfigLookupChild);
}

auto PProxyConfigLookupChild::Manager() const -> PSocketProcessChild*
{
    return static_cast<PSocketProcessChild*>(IProtocol::Manager());
}

auto PProxyConfigLookupChild::AllManagedActors(nsTArray<RefPtr<mozilla::ipc::ActorLifecycleProxy>>& arr__) const -> void
{
    uint32_t total = 0;
    arr__.SetCapacity(total);

}

auto PProxyConfigLookupChild::RemoveManagee(
        int32_t aProtocolId,
        IProtocol* aListener) -> void
{
    FatalError("unreached");
    return;
}

auto PProxyConfigLookupChild::DeallocManagee(
        int32_t aProtocolId,
        IProtocol* aListener) -> void
{
    FatalError("unreached");
    return;
}

auto PProxyConfigLookupChild::OnMessageReceived(const Message& msg__) -> PProxyConfigLookupChild::Result
{
    switch (msg__.type()) {
    case PProxyConfigLookup::Msg___delete____ID:
        {
            if (mozilla::ipc::LoggingEnabledFor("PProxyConfigLookupChild")) {
                mozilla::ipc::LogMessageForProtocol(
                    "PProxyConfigLookupChild",
                    OtherPid(),
                    "Received ",
                    (&(msg__))->type(),
                    mozilla::ipc::MessageDirection::eReceiving);
            }
            AUTO_PROFILER_LABEL("PProxyConfigLookup::Msg___delete__", OTHER);

            PickleIterator iter__{msg__};
            PProxyConfigLookupChild* actor{};
            nsTArray<ProxyInfoCloneArgs> aProxyInfo{};
            nsresult aResult{};

            if ((!(ReadIPDLParam((&(msg__)), (&(iter__)), this, (&(actor)))))) {
                FatalError("Error deserializing 'PProxyConfigLookup'");
                return MsgValueError;
            }
            if ((!((*((&(actor))))))) {
                FatalError("Error deserializing 'PProxyConfigLookup'");
                return MsgValueError;
            }
            // Sentinel = 'actor'
            if ((!(((&(msg__)))->ReadSentinel((&(iter__)), 102892058)))) {
                mozilla::ipc::SentinelReadError("Error deserializing 'PProxyConfigLookup'");
                return MsgValueError;
            }
            if ((!(ReadIPDLParam((&(msg__)), (&(iter__)), this, (&(aProxyInfo)))))) {
                FatalError("Error deserializing 'ProxyInfoCloneArgs[]'");
                return MsgValueError;
            }
            // Sentinel = 'aProxyInfo'
            if ((!(((&(msg__)))->ReadSentinel((&(iter__)), 370344976)))) {
                mozilla::ipc::SentinelReadError("Error deserializing 'ProxyInfoCloneArgs[]'");
                return MsgValueError;
            }
            if ((!(ReadIPDLParam((&(msg__)), (&(iter__)), this, (&(aResult)))))) {
                FatalError("Error deserializing 'nsresult'");
                return MsgValueError;
            }
            // Sentinel = 'aResult'
            if ((!(((&(msg__)))->ReadSentinel((&(iter__)), 185205473)))) {
                mozilla::ipc::SentinelReadError("Error deserializing 'nsresult'");
                return MsgValueError;
            }
            msg__.EndRead(iter__, msg__.type());
            if ((!((static_cast<ProxyConfigLookupChild*>(this))->Recv__delete__(std::move(aProxyInfo), std::move(aResult))))) {
                mozilla::ipc::ProtocolErrorBreakpoint("Handler returned error code!");
                // Error handled in mozilla::ipc::IPCResult
                return MsgProcessingError;
            }


            IProtocol* mgr = actor->Manager();
            actor->DestroySubtree(Deletion);
            actor->ClearSubtree();
            mgr->RemoveManagee(PProxyConfigLookupMsgStart, actor);

            return MsgProcessed;
        }
    default:
        return MsgNotKnown;
    }
}

auto PProxyConfigLookupChild::OnMessageReceived(
        const Message& msg__,
        Message*& reply__) -> PProxyConfigLookupChild::Result
{
    return MsgNotKnown;
}

auto PProxyConfigLookupChild::OnCallReceived(
        const Message& msg__,
        Message*& reply__) -> PProxyConfigLookupChild::Result
{
    MOZ_ASSERT_UNREACHABLE("message protocol not supported");
    return MsgNotKnown;
}

auto PProxyConfigLookupChild::ClearSubtree() -> void
{
}



} // namespace net
} // namespace mozilla
namespace mozilla {
namespace ipc {
auto IPDLParamTraits<mozilla::net::PProxyConfigLookupChild*>::Write(
        IPC::Message* aMsg,
        mozilla::ipc::IProtocol* aActor,
        const paramType& aVar) -> void
{
    int32_t id;
    if (!aVar) {
        id = 0;  // kNullActorId
    } else {
        id = aVar->Id();
        if (id == 1) {  // kFreedActorId
            aVar->FatalError("Actor has been |delete|d");
        }
        MOZ_RELEASE_ASSERT(
            aActor->GetIPCChannel() == aVar->GetIPCChannel(),
            "Actor must be from the same channel as the"
            " actor it's being sent over");
        MOZ_RELEASE_ASSERT(
            aVar->CanSend(),
            "Actor must still be open when sending");
    }

    WriteIPDLParam(aMsg, aActor, id);
}

auto IPDLParamTraits<mozilla::net::PProxyConfigLookupChild*>::Read(
        const IPC::Message* aMsg,
        PickleIterator* aIter,
        mozilla::ipc::IProtocol* aActor,
        paramType* aVar) -> bool
{
    mozilla::Maybe<mozilla::ipc::IProtocol*> actor =
        aActor->ReadActor(aMsg, aIter, true, "PProxyConfigLookup", PProxyConfigLookupMsgStart);
    if (actor.isNothing()) {
        return false;
    }

    *aVar = static_cast<mozilla::net::PProxyConfigLookupChild*>(actor.value());
    return true;
}

} // namespace ipc
} // namespace mozilla
